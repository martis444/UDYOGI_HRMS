# UDYOGI HRMS — Build Progress
*Claude Code reads this automatically. Update before every /exit.*

---

## Current Status
**Session 19 (live attendance/payslip onboarding + PF-ESIC fix + bulk exports) ✅ (2026-06-27) — driven by the client running real attendance/payroll on the live box.** Sessions 18/18b/18c are now DEPLOYED (016+017 applied; the 3 new cols confirmed on the live DB). Work this session, all pushed to `main`:
  • **Salary import — comma/currency numbers** (`6fc9154`): `_row_dec` did `Decimal("8,750.00")` → threw on the comma → silently `None`, so basic/ctc dropped out while plain `750` survived (employees showed blank salary). Now strips commas/₹/Rs/spaces before parsing.
  • **Attendance file decode** (`25e732d`): `parse_attendance_csv` used `pd.read_csv` with no encoding handling → a `₹`/accented name in an Excel ANSI/BOM CSV raised `UnicodeDecodeError` → **500** on `/attendance/import/validate`. Now decodes utf-8-sig→latin-1, accepts `.xlsx`, returns a clean 400 on unreadable files.
  • **Attendance import UX** (`5fb0375`, `2e59bbd`): list the validate **warnings** (row+emp+issues), make the template download **optional** (upload your own file directly), accept `.xlsx`; show the **target month** in the import panel and **persist** selected period/entity to localStorage (the tab was resetting to the current month on remount, making a different month's data look "deleted").
  • **Payslip zeroing on generate** (`6ad5559`): viewing/generating a payslip calls `process_payroll_month` (payslip `/data` reprocesses unlocked rows), which recomputed days from the empty per-day table and wiped CSV attendance → all months showed full pay. Fix: when there are no per-day `attendance_daily` rows but an existing row carries uploaded attendance, **preserve it** (uploaded `pay_days` drives /30 proration); per-day data still wins. Verified upload 27/3-absent → stays after reprocess (net 7558).
  • **Employee detail fixes** (`3ba7069`, `b4fa779`): multi-number mobile overflowed the header grid into Monthly-gross (now `break-words`+`min-w-0` on InfoRow); detail page showed raw `department_id` (e.g. "24")/grade/shift/location ids → now resolved to names via the loaded form options.
  • **PF/ESIC on the PAID amount** (`86a3f76`) — client rule (the "₹25,000, take 2 days off → paid for 28" example): PF/ESIC were charged on the full committed gross even with LOP. Now in `process_payroll_month`, after the /30 factor: PF on prorated basic (caps ₹1,800/₹2,340), ESIC on prorated gross (eligibility still on committed gross ≤ ₹21,000), PT unchanged (committed-gross slab); `total_deduction` rebuilt. Full month unaffected — **golden UM000001 9349/785/71/0/8493 PASS**; LOP 3/30 → PF 785→707, ESIC 71→64, net 7558→7643.
  • **Bulk exports** (`6b2d63f`, `4d3859e`) — Payroll page, per processed/locked month row, admin/super-admin, entity-scoped, audited: **Bulk payslip PDF** (`GET /api/payslip/bulk-pdf`, all employees merged one-per-page via `generate_bulk_pdf` — logo dedupes so it scales) and **Salary sheet as `.xlsx`** (`GET /api/payslip/salary-sheet`, openpyxl register: paid days, earnings, gross, PF/ESIC/PT/LD/Loan, total-ded, net, employer PF/ESIC + bold totals row, frozen header, A3-landscape fit-to-width print setup; ~6 KB). Both reuse `_build_response` so figures reflect the paid-amount math. tsc + next build + py-compile clean throughout; both outputs verified rendering/opening for UMPL.
**Session 18c (attendance CSV authoritative) ✅ (2026-06-25).** The monthly attendance CSV import writes aggregate day-counts (P/A/L/…/pay_days) straight to `payroll_months` but no per-day `attendance_daily` rows. The 15.10 monthly-summary self-refresh reprocessed *every* non-locked row from `attendance_daily`, recomputing days_a=0 for CSV-only months → it **wiped uploaded attendance** (verified: upload 3 absent → payslip net 6658 ✓, but a summary view reset it to pay_days 30 / net 8493). Fix: the self-refresh now only reprocesses employees that actually **have per-day `attendance_daily` rows** in the 26→25 window (biometric punches / reflected approved leaves); CSV-only months are left authoritative. Payslip `/data` already never reprocessed (reads the stored snapshot), so upload→payslip proration is stable. **Attendance CSV header:** `Emp Code, Employee Name, Total Days, Pay Days, P, A, L, R, C, PL, S, H, LT, OT Hours, Salary Flag, Remarks`; pay_days drives /30 proration. Backend-only (no migration).
**Next session / deploy state:** 016+017 are LIVE; Session-19 commits up to `6ad5559` were pulled+deployed step-by-step on the live box. **NOT yet deployed: `86a3f76` (PF/ESIC paid-amount), `6b2d63f` + `4d3859e` (bulk payslip PDF + xlsx salary sheet)** — needs `git pull` → `npm run build` → `nssm restart UdyogiBackend` + `UdyogiFrontend`. **HR still must set `confirmation_date`** on existing employees or CL/SL stay 0 (PL unaffected). Open/asked-later: ZIP of individual payslips (for per-employee email); salary-sheet column tweaks / statutory IDs (UAN/ESIC/PF no.) for filing; revisit PL accrual cap (15.7 OPEN, now the only uncapped bucket); the env note — local dev DB diverges from live (re-create from `schema_snapshot.sql`, don't assume parity). Deploy cycle reminder: `git pull` doesn't reload NSSM services — always `nssm restart` after; on Windows answer `n` to git's pack-unlink (gc) prompt (harmless, antivirus lock).
**Session 18b (leave rule: CL/SL no carry-forward) ✅ (2026-06-25) — client rule.** CL & SL no longer carry forward; they **accrue monthly within the financial year and reset each 1 April**. PL is unchanged (cumulative, granted after 1y DOJ, carries forward — now the only uncapped bucket). New model in `leave_engine.resolve_leave_balance` (return shape `{cl,sl,pl:{tb,ulb,alb}}` unchanged → zero consumer changes): **CL/SL** tb = `min(completed_confirmed_months × policy/12, policy)` (fractional 2dp, arrears — credited *after* each completed month), used = CL/SL consumed **within the current FY only**; **PL** tb = `years × policy` cumulative, used all-time. Accrual starts after a new **HR-set `confirmation_date`** (migration `017_confirmation_date.sql`; distinct from the auto `probation_end_date`) — CL/SL = 0 until confirmed. FY boundary = `config.FINANCIAL_YEAR_START_MONTH=4` (1 Apr–31 Mar). Because balances are DERIVED (15.7), the FY reset needs **no stored mutation** — CL/SL are just scoped to the current FY on read; `_used_days` gained an optional [start,end] window (CL/SL pass the FY window, PL passes none). `confirmation_date` wired through model/schemas/create+detail/export, admin detail+export+column-update (added to `_DATE_COLS`), import (mapping + insert + `_apply_updates`), add/edit forms, bulk-import + column-update UIs. **Tests:** pure accrual (confirmed-before→2.5 CL by Jul15; full 10 by 31 Mar; **0 on 1 Apr = reset**; mid-FY Aug10→3.33 by Dec31; unconfirmed→0; Apr1 confirm→0.83 by Apr30) all PASS; DB resolve on UP000001 (6y) → CL 1.67 / SL 1.17 (2 FY months) but **PL still 84** (cumulative) PASS; golden UM000001 9349/785/71/0/8493 unchanged (leave doesn't touch net); tsc + next build clean; schema re-dumped. **Bundled with 016 in one deploy.**
**Session 18 (employee field changes) ✅ (2026-06-25) — client-requested column add/remove on `employees`.** Migration `016_employee_fields.sql` (one atomic txn): (1) **`category` CHECK widened** staff/worker → **director|staff|worker** (on `employees` + `employee_categories`); director behaves like staff for leave (only `worker` is gated to 0 — leave_engine unchanged). (2) **ADD** `profit_center_code/name`, `cost_center_code/name` (varchar), `resignation_date` (date, distinct from `exit_date`), and **`retirement_date` as a generated column** `((dob + '60 years')::date) STORED` — read-only, auto, NULL when dob NULL (verified UM000001 dob 1990-05-15 → 2050-05-15). (3) **REMOVE** `present_city/state/pin`, `perm_city/state/pin`, `bank_branch` (full `present_addr`/`perm_addr` text kept) — dropped+recreated `v_employee_full` first (it referenced present_city/state, same view-dependency gotcha as 013). (4) **Wired LTA/Medical/Others-earning** (existing DB cols `leave_travel`/`medical`/`other_earning` — `medical`/`other_earning` were schema-only since 13.20, never in model/forms) into model + schemas + create/detail/export + import + add/edit forms. (5) **`other_allowance` repurposed to RECORD-ONLY** per client: removed `+ other_allowance` from both engine net paths (`payroll_engine` compute + prorated net), removed the OTHER ALL. earnings row from the PDF template + payslip page, and dropped the other-allowance input/added-back line from the statutory calculator. NO net-pay impact (0 employees, 0 payroll rows had it >0). Repurpose verified: set other_allowance=500 on UM000001 → net stayed 8493. **Touchpoints:** model (`FetchedValue` for the generated col), create/update/list/response schemas, `employees.py` (detail dict + create + CSV export headers/rows), `admin.py` (detail dict + CSV export + column-update `ALLOWED_COLUMNS`/`_NUMERIC_COLS`/new `_DATE_COLS` for resignation_date), `import_service.py` (col mapping, `_TEXT_COLS`, `_apply_updates` numeric+category+date, INSERT), add form + `[emp_code]` detail/edit (category dropdown, profit/cost center, medical/other_earning/other_allowance fields, resignation date, read-only retirement date, address collapsed to single full-text field), column-update + bulk-import template UIs (also removed the stale `da` column-update entry from 15.1). **Golden UM000001 Apr 2026 = 9349/785/71/0/8493 STILL PASS**; tsc + `next build` clean; py-compile clean; app imports + model reads OK; schema re-dumped (4 new cols present, 0 removed cols remain). Running container/services have OLD code — deploy to apply.
**Session 17 (live bugfixes + SAP code) ✅ (2026-06-23) — found while the client onboarded employees via bulk import on the live server.** Worked directly against the live Windows box (foreground-uvicorn to read tracebacks; each fix revealed the next bad value since one bad row rolls back the whole atomic batch). **Bulk-import chain fixed:** (a) gender CHECK — `M` was stored as `m`; map m/f/o→male/female/other in importer + create/update schemas. (b) mobile "invalid" — stray/zero-width spaces; strip ALL whitespace before validating. (c) mobile NOT NULL — directors have no phone; **made mobile OPTIONAL** (migration `014_mobile_optional.sql` drops NOT NULL; schemas/import/UI relaxed; default login password falls back to last-4 of emp_code when no mobile). (d) value-too-long — server was missing migration `013` (mobile varchar(15)); **rewrote 013 to drop+recreate v_employee_full** (a view depended on the column, blocking ALTER) then widen to (64). (e) duplicate legacy_code='0' — `0` is a placeholder vs a UNIQUE column; treat blank/`0` as NULL + validate real legacy dups in the dry-run. (f) report crash — `_row_label` did `.get(k,"")` on a key now present-with-None. (g) department showed blank — `department_id` is an int FK but HR types the NAME; importer now **resolves department name→id within the entity, auto-creating new names** (cached per batch). **UPSERT added:** re-uploading now UPDATES existing employees (matched by emp_code or legacy_code) instead of erroring "already exists"; inserts+updates in one atomic batch; blank cells never overwrite (rule 7); emp_code never touched (rule 3); esic recomputed on salary change (rule 4); response/UI report created+updated counts. **Migration audit:** diffed live DB vs schema_snapshot via `pg_dump`+`git diff` — only cosmetic pg_dump 18.3-vs-18.4 noise, all tables/columns present (013/014 were the only real gaps). **SAP CODE feature (migration `015_sap_code.sql`):** `sap_code` on employees only (single source of truth; unique-when-present partial index; v_employee_full recreated to expose it). Surfaced everywhere — model + create/update/list/response schemas, list+export endpoints (returned, searchable, +new `designation` filter), bulk import (col mapping, insert/upsert, in-file & DB uniqueness validation), column-update allowed cols, add/detail forms, and a **SAP code column in the employee table**. Added **per-column filters** to the employee list: Location (dropdown), Department + Designation (text), Status (existing); search now matches name/emp/legacy/SAP. Also fixed the stale SQLAlchemy `mobile` column def (was NOT NULL String(15)). tsc clean; all backend syntax-checked. Pushed through commit 1bcec96. **Deploy needs migrations 013(if not yet)/014/015 via psql + frontend rebuild + service restart.**
**DEPLOYMENT LIVE ✅ (2026-06-22) — NATIVE WINDOWS SERVER 2016 (no Docker).** App runs natively on the client's physical Server 2016 box at `C:\UdyogiHRMS1`: PostgreSQL 18 native + uvicorn (127.0.0.1:8000) + `next start` (127.0.0.1:3000) + **native Caddy** reverse proxy on **:8080** (single same-origin front door → no CORS). Reachable by office LAN at **http://10.0.101.1:8080** (server's real NIC; the 192.168.56.1 it also shows is a leftover VirtualBox host-only adapter — ignore). All three run as **NSSM Windows services** (`UdyogiBackend`/`UdyogiFrontend`/`UdyogiCaddy`, auto-start on boot). **DB brought current** by manually applying migrations `009`→`013` via psql (server was stuck pre-15.1 with the `da` column; `git pull` never touches the DB and the seed dump is gitignored). **WeasyPrint** needs GTK on Windows: installed the GTK3 runtime (`C:\Program Files\GTK3-Runtime Win64\bin`) — MSYS2 was unusable (latest drops Server 2016 support). The backend *service* (LocalSystem) needs that folder on its PATH via `nssm set UdyogiBackend AppEnvironmentExtra "PATH=...GTK...;..."`. Frontend built same-origin: `frontend/.env.local` → `NEXT_PUBLIC_API_URL=` (empty), because api.ts falls back to `http://localhost:8000` when unset. Native Caddy config = `Caddyfile.windows` (the committed `Caddyfile` targets Docker hostnames). Superadmin reset via `scripts/create_superadmin.py` → UP000001 / `Udyogi@2026`. **Update cycle now:** `git pull` → (frontend changed) `npm run build` → (schema changed) run new migration via psql → `nssm restart Udyogi*`.
**Session 16 bugfixes (deploy) ✅ (2026-06-22) — found while bringing the live system up.** (a) **change-password** 422+crash: frontend sent only current/new; backend requires `confirm_password` (commit 4bb9eef). (b) **bulk-import** crash + broken: validate returned valid_count/error_count/invalid but UI reads total_valid/total_error/errors; commit returned `imported` vs UI `created` — reshaped both in the API layer (8ba0d90). (c) **multiple mobile numbers**: now accept 1+ 10-digit numbers slash-separated (`9876543210/9123456780`), shared validator across importer + create/update schemas, `employees.mobile` widened varchar(15)→(64) (migration `013`, e16e36b). (d) **import template guidance**: added a "how to fill the columns" guide on the bulk-import page (emp_code blank→auto-gen, old code→`legacy_code`, date/mobile/entity formats). Client confirmed E0204-style codes are treated as **legacy_code** (emp_code auto-generated, UDYOGI prefix scheme unchanged). Native Caddyfile committed (9af5c80).
**Session 15.10 (bugfix) complete ✅ (2026-06-20) — ATTENDANCE MONTHLY-SUMMARY SELF-REFRESH. After 15.9, approved leaves were in attendance_daily (daily view ✓) but the monthly attendance TABLE was blank — `/monthly-summary` read the stale `payroll_months` snapshot (UP000001 June was processed BEFORE the leaves were reflected, so days_cl/days_pl were NULL). Fix: `/monthly-summary` now self-refreshes non-locked months from attendance_daily before returning (reprocess-on-read, same pattern as payslip `/data`) — reprocesses existing non-locked rows AND employees with attendance in the period's 26→25 window but no row yet (covers leaves routed across the 26th). Locked months stay frozen. Result: UP000001 June → CL 2 / PL 6, July → PL 1 (26-Jun routed to next period); golden 9349/785/71/0/8493 unchanged. Backend-only (frontend reads the same endpoint). NOTE: this reprocesses on each summary GET (idempotent; fine at current scale — revisit for large entities). Container self-heals on rebuild.**
**Session 15.9 (bugfix) complete ✅ (2026-06-20) — DERIVE `used` + RECONCILE APPROVED LEAVES (fixes UP000001 stale data). Reported: UP000001 had 3 approved leaves (CL 2d + PL 7d) but used=0 and no attendance reflection. Root cause: those were approved 2026-06-16 BEFORE the 15.3 leave→attendance reflection existed, and 15.7 Part D reset used=0 (wrongly assuming directors had no real leave history). Fix (extends 15.7's self-correcting model to `used`): `used` is now DERIVED = count(attendance_daily cl/sl/pl) + LATE_COVER ledger (leave_engine.derived_used); `resolve_leave_balance` write-throughs used + taken_ytd; `reflect_all_approved_leaves` backfills attendance_daily from approved leave_requests; `materialize_all_leave_balances` (startup/daily/force-refresh) now reflects approved leaves then derives → self-heals. Removed the per-event `used` increments (both approve endpoints + late_service) — they'd double-count now (approve still reflects; late still writes the LATE_COVER ledger). Ran materialize → UP000001 now CL 2/60, PL 7/84 + 9 attendance rows (Sun 21-Jun skipped). Golden 9349/785/71/0/8493; regression (fresh approve, late-cover, payslip==resolve) all pass. No schema change. NOTE: encash_pl still subtracts from entitlement which is now derived (pre-existing since 15.7) — out of scope. Running container self-heals on rebuild (startup materialize).**
**Session 15.8 complete ✅ (2026-06-20) — USED-LEAVES CARD + PRE-MARKED ATTENDANCE TEMPLATE + APPROVED-LEAVE IMPORT GUARD. (A) Dashboard "Used leaves" card next to the apply/balance card — shows ULB per type as used/total ("SL 3 / 7") + total, read from the SAME 15.7 shared Zustand cache (`useLeaveBalance`) so it invalidates/refetches with the balance card on apply/approve. (B) Attendance template (`GET /api/attendance/template`) now pre-marks APPROVED leaves: the template is aggregate-count format (no per-day cells), so it pre-fills the C/PL/S count columns with approved-leave day counts for the period's **26→25 window** (matches payslip; NOT get_period_dates which is now calendar-month) and lists the exact dates in Remarks ("APPROVED (do not edit): CL 10-Jun") + a legend; source = attendance_daily cl/sl/pl rows written by approval (15.3). (C) Import guard: re-upload that leaves the pre-marked counts as-is = no-op; reducing a bucket below its approved-in-window count → kept at approved + a warning in the commit result ("UM…: uploaded CL=0 but 1 approved (10-Jun) — kept CL=1"); blank still skipped. ALSO removed the import's legacy leave-`used` deduction (was a double-count vs 15.7's single source — `used` is mutated only at approval now). Frontend surfaces the warnings in the import result. **Acceptance:** 10-Jun CL → June template; 27-Jun CL → July template (28-Jun is a Sunday → reflection skips it, used 27); Used card ULB == balance ULB == payslip ULB (same resolver); unchanged re-upload no-op, conflict warns+keeps; golden 9349/785/71/0/8493; tsc + next build clean. No schema change (snapshot unchanged). Running container needs rebuild to apply.**
**Session 15.7 complete ✅ (2026-06-20) — SELF-CORRECTING LEAVE BALANCES (revises 15.3/15.4 grant). entitlement is now DERIVED (completed_years × policy), never set by hand. New `leave_engine.resolve_leave_balance(emp, as_of, db, write_through)` is the ONE source of truth → {cl,sl,pl:{tb,ulb,alb}}: tb=years×policy (precise anniversary math, not /365; 0 for workers/pre-1yr), ulb=stored cumulative used, alb=max(tb−ulb,0). Policy = single editable `leave_policy` table (migration 012, seed CL10/SL7/PL14, all entities; GET/PUT `/api/leave/policy`, super_admin). Auto-materialized 3 ways (all idempotent): on-read write-through (resolve updates stored entitlement + ensures CL/SL/PL rows), FastAPI startup + daily asyncio refresh (`materialize_all_leave_balances` — no new dep vs APScheduler), and on employee create / DOJ change (`ensure_leave_rows`). EVERY consumer rerouted through resolve: GET /balance, payslip `_build_response` TB/ULB/ALB block, late-deduction highest-bucket lookup (late_service), leave-approval + apply availability checks. `used` still mutated only by real events (approval, late-cover). `/run-annual-grants` demoted to a manual force-refresh. **Part D cleanup:** reset bogus test used/taken_ytd=0, ensured CL/SL/PL rows (UM000001 PL row created), write-through entitlement → UM000001 reads 10/7/14, UP000001 60/42/84 (6yr). **Frontend:** shared Zustand leave-balance store (one cache per emp_code, refetchOnWindowFocus, invalidate-on-apply) replaces the two drifting fetches; chips now show available/total ("CL 8 / 10"). **Acceptance 1-9 pass:** 1yr+1d→10/7/14; 6mo→0; 2yr+CL used3→TB20/ULB3/ALB17; policy CL10→12→TB jumps no migration; UM000001/UP000001 clean; payslip==/balance==late==approval; golden 9349/785/71/0/8493; tsc+next build clean; schema re-dumped; deploy seed (00_init.sql) rebuilt. **OPEN: accrual is UNCAPPED (carry-forward forever) — UP000001 already 84 PL; client to decide a cap (one-line clamp in resolve).** Running container has OLD code (rebuild to apply).**
**Session 15.4 complete ✅ (2026-06-20) — LATE COLUMN + LATE→ABSENT (floor/3) + LEAVE COVER + LD + ADMIN OVERRIDE. Config `LATE_DAYS_PER_ABSENT=3`. Migration `011_late_ld.sql`: payroll_months +late_days/absent_from_late/ld/ld_overridden/late_absent_overridden; **rebuilt GENERATED total_deduction = pf+esic+pt+loan+other_deduction+ld** (drop+readd; model Computed string updated to match; v_payslip_summary recreated). New `services/late_service.py` `compute_late_effects`: late_days = 'late' att_status in the 26th-cycle window; absent_from_late = floor(late/3); cover from HIGHEST CL/SL/PL balance first (spill to next), decrement that bucket's used; uncovered → ld = round(uncovered*gross/30,2). Idempotent per period via a `LATE_COVER:{emp}:{y-m}` ledger in leave_accrual_log (reconcile delta, never blind re-subtract). Engine integrates it (uncovered late charged ONCE as LD, NOT also folded into /30 LOP — LOP stays real absent+lwp). Admin override `POST /api/payroll/late-override {emp_code,year,month,absent_from_late?,ld?,reason}` (super/entity admin, 400 if locked, reason≥4, sets flags + reconciles leave + reprocess, audit `LATE_LD_OVERRIDE`). Payslip: LD row (15.2) now carries real value, total_deduction/net reflect it, + a "late_days→absent-equiv" note (PDF + frontend). Frontend: payslip admin Late/LD card + Edit modal (unlocked only); attendance summary/import/template gain an **LT (Late)** column (+ template E→PL cleanup from 15.3); api.ts `apiLateOverride`. **Acceptance:** 6 late + CL5 → absent2, cover 2 CL, ld0; 6 late + no balance → uncovered2, ld600; 5→1/3→1/2→0; override ld sticks across reprocess + audited, locked→400; golden UM000001 9349/785/71/0/8493 ld0 total_deduction 856; tsc + next build clean; PDF renders; schema re-dumped. Running container has OLD code (rebuild to apply).**
**Session 15.3 complete ✅ (2026-06-20) — FLAT ANNUAL LEAVE GRANT + EL RETIRED + LEAVE→ATTENDANCE. Leave model is now CL/SL/PL only (EL retired; folded into PL). Replaced monthly accrual (2/1, 1.167/mo, probation-gated) with a FLAT annual GRANT: config `ANNUAL_LEAVE={CL:10,SL:7,PL:14}`; `grant_annual_leave(emp,as_of)` grants on each DOJ anniversary (year_index=(as_of−doj)//365; <1 → no balance), idempotent per `ANNUAL_GRANT:{emp}:{idx}`, carries unused balance forward + adds the quota + resets used=0; `run_grants` batches. Probation decoupled from leave (kept for confirmation only). `encash_pl` kept. Migration `010_leave_buckets.sql`: fold EL→PL, leave_balances/leave_policies CHECK → {CL,SL,PL}, attendance_daily att_status 'el'→'pl', `payroll_months.days_el`→`days_pl` (+ v_payslip_summary recreated). Endpoints: `/run-monthly-accrual`→`/run-annual-grants`; `/balance` returns CL/SL/PL `{tb,ulb,alb}` (+entitlement/used/balance aliases). **Leave→attendance (Part C):** approving a leave now writes each working day into attendance_daily as paid (cl/sl/pl) + bumps leave_balances.used; idempotent per (emp,date). The payroll engine's attendance query switched to the **26th pay-cycle window** (period (y,m) = 26th-of-prev-month .. 25th-of-this-month) so a leave/absence on/after the 26th lands in next month's run and a span across the 26th splits — done in ONE place (engine window), not per-call maths. `_count` also now counts `days_pl`. **Acceptance:** doj 2025-06-16 → grant on 2026-06-16 = CL10/SL7/PL14 used 0 (before → no balance); 2nd anniversary with 4 PL unused → PL ent 18; EL gone (DB checks, code, UI; grep clean); leave 24-25→June window, 26-27→July window (boundary split); PL day not LOP (2 pl + 2 absent → pay_days 28); golden UM000001 9349/785/71/0/8493; tsc + next build clean; schema re-dumped. Running container has OLD code (rebuild to apply).**
**Session 15.2 complete ✅ (2026-06-20) — PAYSLIP RESTRUCTURE + CALCULATOR + EMPLOYEE-TAB FIELD REMOVALS. Payslip (PDF template + frontend PayslipDocument, kept in lockstep): removed the Authorised-Signatory block + the DA row; earnings now ALWAYS render Basic/HRA/Special/CCA/LTA (CCA + LTA no longer conditional), Other Allowance conditional; footer shows labelled GROSS (Rate) + Total Earnings + Total Deductions + NET PAY (both the words GROSS and NET appear); deductions keep PF/ESIC/PT/Loan and add an LD (Late Deduction) row rendered when ld>0 (value wired in 15.4; `_build_response` exposes `ld`=0 for now); leave block replaced with a TB/ULB/ALB × CL|SL|PL grid sourced from LeaveBalance (entitlement/used/balance; missing bucket→0/0/0). `_build_response` adds `ld` + structured `leave` block. Statutory calculator: added LTA + Other-allowance inputs; GROSS (basic+hra+spl+cca+lta) and NET (gross−PF−ESIC−PT+other) both shown prominently+labelled; PF note '12% of basic, cap ₹1,800' + 'cap reached when basic ≥ ₹15,000', employer '13% of basic, cap ₹2,340'; per-day helper '1 day = gross ÷ 30'. Employee ADD/EDIT/detail: removed PF-applicable + PT-applicable toggles (DA already gone in 15.1; esic stays auto/internal) from UI + create/update payloads; added LTA (leave_travel) as a labelled field in add+edit + live-gross + (IncrementModal already had it). Golden UM000001 Apr 2026 STILL 9349/785/71/0/8493; PDF renders (WeasyPrint, 1.9MB) with no signature/DA, CCA+LTA visible, GROSS+NET labelled, leave TB/ULB/ALB (CL 12/0/12, SL 6/0/6, PL 0/0/0); tsc + next build clean. Running container has OLD code (rebuild to apply).**
**Session 15.1 complete ✅ (2026-06-20) — REMOVE DA + REBASE PF + /30 PRORATION. DA folded into basic everywhere (UM000001 basic 5542+1000=6542) and the `da` column dropped from employees/salary_structures/payroll_months (migration `009_remove_da.sql` + backup `backup_pre_15_1_da.sql`; the 2 views that referenced da were dropped+recreated da-free). Gross = basic+hra+spl+cca+leave_travel (da gone); PF base = basic only (12% cap 1800 / 13% cap 2340). Proration switched from pay_days/total_days (and /26) to a CONFIG /30 model: `PER_DAY_DIVISOR=30`, `CYCLE_CUTOFF_DAY=26` in config.py; payable_factor=(30−LOP_days)/30 where LOP_days=days_a+days_lwp; earnings prorate by factor (other_allowance always full), deductions never prorated; pay_days stored = 30−LOP. Also fixed `_count` att_status to the real lowercase DB codes (present/absent/lwp/…) so LOP proration can actually fire (was uppercase 'A'/'LWP' — never matched). Golden UM000001 Apr 2026 STILL 9349/785/71/0/8493; synthetic LOP test (gross 9000, 3 absent days → 8100 earnings, 900 reduction) verified+reverted; zero `da` refs in backend+frontend; tsc + next build clean; schema re-dumped. Running container still has OLD code (rebuild to apply).**
**Session 14.15 complete ✅ (2026-06-18) — Nav cleanup: removed "More" from mobile BottomNav (+ unused MoreHorizontal import) and "Master data" from the Sidebar tab + dashboard quick-action tile. Nav-only (master-data page + /api/admin/master-data endpoint still exist, just unlinked). tsc clean; pushed c21f73a.**
**Session 14.14 complete ✅ (2026-06-18) — PAYROLL PERIOD MODEL CHANGED per client rule: 26th→25th cycle → CALENDAR MONTH (1st→last day); increments now effective from the 1st (was 26th). One month = one rate, no mid-month split (month granted stays old rate, new rate from 1st of next month). Backend period_calculator/increment_service/salary_resolver/payroll_engine/employees/loan_service + frontend IncrementModal/about. Golden UM000001 Apr 2026 STILL 9349/785/71/0/8493 ✅; increment guard 1st=accept, 26th/15th=reject; tsc clean. Running container still has OLD code (rebuild to apply).**
**Session 14.13 complete ✅ (2026-06-18) — Browser login bugfix in the Docker stack: `NEXT_PUBLIC_API_URL=/api` caused double-prefix `/api/api/auth/login` → 404. Fixed: build arg → EMPTY (same-origin) + api.ts `||`→`??` so empty base sticks; frontend now calls `/api/auth/login` via Caddy. Browser login UP000001/Admin@2026 CONFIRMED working on :8080. Pushed (7e5a5b3). PDF still to verify.**
**Session 14.12 complete ✅ (2026-06-18) — LOCAL Docker stack VALIDATED end-to-end on :8080 (off the office proxy network). Build green; 4 containers healthy; test DB loaded from 00_init.sql (11 emp/6 loans); /api/health ok; /login 200; golden UM000001 Apr 2026 = 9349/785/71/0/8493 intact in-container. Fixes pushed: postgres:16→18 + volume path /var/lib/postgresql (PG18); dropped libgdk-pixbuf2.0-0 (renamed in Debian trixie). NEXT: real VPS deploy (clone + copy 2 gitignored files + ufw 8080 + deploy.sh).**
**Session 14.11 complete ✅ (2026-06-18) — Created missing deploy files (frontend/Dockerfile standalone, next.config output:'standalone', backend/.env.production, root .env, .dockerignores, /api/health alias), git-initialized the project and pushed to PRIVATE GitHub repo github.com/martis444/UDYOGI_HRMS (secrets/DB-dump/node_modules excluded; env templates included). LOCAL docker build BLOCKED by office TLS-inspecting proxy (192.168.0.254:8090) → decided to build on the VPS instead. Deploy NOT yet run on server.**
**Session 14.10 complete ✅ (2026-06-18) — Built the deploy test-DB seed: `backend/sql/init/00_init.sql` (full schema+data dump of local udyogi_hrms; 25 tables, pgcrypto, 11 employees, 6 dummy loans; no CREATE DATABASE → Docker-init safe). For the container's `/docker-entrypoint-initdb.d` on first boot. NOTE: deploy configs (docker-compose.prod.yml / Caddyfile / deploy.sh / .env.production) still NOT created — see DEPLOY_GUIDE_TEST.md.**
**Session 14.9 complete ✅ (2026-06-18) — Loans tab polish + bugfix: added `.press` to row-action buttons, swapped Close icon Ban→Archive (amber hover, non-destructive semantics). **Bugfix:** info banner was unreadable — near-black text (#1A1A1A) on a 7%-blue panel over the dark dashboard chrome = black-on-dark; fixed to dark-theme banner (text-white/85, light-blue #93C5FD icon, /15 bg + /30 border). tsc + next build + detector clean.**
**Session 14.8 complete ✅ (2026-06-18) — impeccable polish of login page: removed dead controls (no-op "Remember me" + "Forgot password?"→# — reset is admin-only), a11y (label htmlFor/id associations, error role=alert, show/hide aria-label+pressed, button keyboard-focus state), autofocus Employee ID, restored spacing rhythm. tsc + next build + detector clean.**
**Session 14.7 complete ✅ (2026-06-18) — impeccable critique of Loans tab (28/40 Good; detector clean) → applied harden + scoped polish: modals now Esc/backdrop-dismiss/focus-trap/aria (shared `useDialog`), employee-picker dropdown portaled (no clip), a11y (aria-labels + focus rings + aria-live toast), filter search unified to shared INPUT, count contrast bumped. tsc + next build clean.**
**Session 14.6 complete ✅ (2026-06-18) — Post-14.5 final checks + UI fixes: golden test re-verified via live engine (9349/8493), tsc + next build clean, schema_snapshot.sql re-dumped. Fixed LiquidGlassScene InvalidStateError (infinite-animation finish()). Added dev credit + © to login, dev credit to About, © to Payslips. Removed 3 summary cards from Loans tab. Test-row cleanup DEFERRED by user (nothing deleted).**
**Session 14.5 complete ✅ — About tab (all roles); consolidated appMeta.ts into a single `APP_META` object (Credits refactored to it). Version single-source = 1.0.0 (frontend APP_META + backend version.py).**
**Session 14.4 complete ✅ — Credits tab (all roles) + read-only /api/meta/system-stats (admin, entity-scoped). appMeta.ts is the single source for credits text.**
**Session 14.3 complete ✅ — Loan/advance module: loans + per-month editable EMI ledger, auto-deduct on payroll (feeds existing loan_emi → GENERATED total_deduction), Loans admin tab. Golden 9349/8493 intact.**
**Session 14.2 complete ✅ — Seeded the 8 prominent figures (UP000001–UP000008, names locked); name-edit + delete protected (API + DB trigger). Golden 9349/8493 intact.**
**Session 14.1 complete ✅ — Locations (GSTN) module: replaced 9 legacy city-codes with 24 GSTN units, remapped all employees (UM000001→NOIDA, PT stays NIL), + editable Locations admin tab. Golden 9349/8493 intact.**
**Session 13.21 complete ✅ — Removed People + Reports sidebar tabs (schema snapshot re-confirmed current)**
**Session 13.20 complete ✅ — Added missing salary columns to DB (conveyance/medical/other_earning/salary_arrears/income_tax/lwf/nps); schema only, engine not wired; payroll_series + salary_att held**
**Session 13.19 complete ✅ — UI polish from impeccable critique: contrast/legibility (#6B6B6B + near-opaque glass), shared SalaryHistoryTable/IncrementModal/Skeleton, USAPL teal, entity-tab fix**
**Session 13.18 complete ✅ — Bugfix: recharts width(-1)/height(-1) dashboard chart warning (deferred ResponsiveContainer mount)**
**Session 13.17 complete ✅ — Payroll console (Monthly Run: process/lock/unlock + Salary Structures: history + apply-increment moved here)**
**Session 13.16 complete ✅ — Leave tracker simplified to balances-only (encashment + streak UI removed)**
**Session 13.15 complete ✅ — Salary structure history + effective-dated increments (Parts A–H done, acceptance tests H1–H7 passed)**
**Session 13.14 complete ✅ — Public holidays, period calculator, leave request engine (Parts A–H all done + acceptance tests H1–H7 passed)**

---

## Environment
- Local Postgres: running NATIVELY (PostgreSQL 18, EDB installer) at localhost:5432 — NOT in Docker
  - psql binary: `/Library/PostgreSQL/18/bin/psql`
  - User: `postgres`, password: `2399`, database: `udyogi_hrms`
- Docker: daemon running but NO containers active — DB is native, not containerised
- Database: `udyogi_hrms` — schema applied ✓ (001 + 002 SQL files ran + 13.6b migrations)
- Total tables: 23 — full live schema dumped to **`backend/sql/schema_snapshot.sql`** (source of truth; regenerate with `pg_dump --schema-only` after schema changes — command in CLAUDE.md)
- pgAdmin: available via `/Applications/PostgreSQL 18/pgAdmin 4.app`
- Backend: uvicorn running natively at http://localhost:8000 (Python 3.14 venv)
- Frontend: Next.js dev server at http://localhost:3000
- Project folder: `/Users/abhishekdey/UDYOGI_HRMS/`
- Claude Code account: manager's Max plan account
- This chat (planning): Abhishek's personal account

---

## Session Log

| # | Session | Status | What was done | Issues |
|---|---------|--------|---------------|--------|
| 15.10 | Bugfix: attendance monthly-summary self-refresh | ✅ Done | **Reported:** after 15.9 the daily attendance view showed UP000001's leaves but the monthly attendance TABLE was blank. **Cause:** `/monthly-summary` reads `payroll_months.days_cl/days_pl` (snapshot), and UP000001's June row was `processed` BEFORE 15.9 reflected the leaves into `attendance_daily`, so the snapshot stayed NULL; the endpoint never recomputed. **Fix:** `/monthly-summary` now self-refreshes — for the requested period it reprocesses (a) existing non-locked rows and (b) any employee with `attendance_daily` rows in the 26→25 window but no payroll row yet (so leaves routed across the 26th into the next period also surface), then returns the refreshed rows. Locked months stay frozen (skipped). Same reprocess-on-read pattern the payslip `/data` endpoint already uses; added `process_payroll_month` import. **Verified:** UP000001 June → **CL 2 / PL 6**, July → **PL 1** (26-Jun); golden UM000001 9349/785/71/0/8493. Backend-only (frontend grid reads the same endpoint, now correct; expandable daily view was already correct). | Reprocesses on every summary GET (idempotent — loan EMI/late effects/leave derive all idempotent; recomputes net from current salary/attendance for non-locked rows). Fine at current scale; for large entities consider a lighter live-derive or caching. Creating a row for window-attendance employees means viewing the summary can create payroll_months rows for employees with leave but no prior row (expected — they have attendance). |  **Reported:** UP000001 showed 3 approved leaves (CL 2d + PL 7d) but `used=0` and nothing on the attendance sheet. **Root cause:** approved 2026-06-16 (before 15.3's leave→attendance reflection) + my 15.7 Part D reset `used=0` (wrongly treated directors as having no real leave history). **Fix (extends 15.7 self-correcting to `used`):** `used` is now DERIVED via `leave_engine.derived_used()` = count(`attendance_daily` cl/sl/pl) + LATE_COVER ledger; `resolve_leave_balance` write-throughs `used`+`taken_ytd`; new `reflect_all_approved_leaves()` backfills `attendance_daily` from approved `leave_requests`; `materialize_all_leave_balances` (startup/daily/force-refresh) now reflects approved leaves THEN derives → self-heals. Removed per-event `used` increments in both approve endpoints + `late_service` (would double-count; approve still reflects to attendance, late still writes the LATE_COVER ledger — both are the authoritative sources `used` derives from). Ran materialize → **UP000001 now CL 2/60 (bal 58), PL 7/84 (bal 77), 9 attendance rows** (Sun 21-Jun correctly skipped). **Verified:** golden 9349/785/71/0/8493; regression — fresh approve → derived used; late-cover → ledger-derived used; payslip leave block == resolver. No schema change. | New approvals always worked (15.8 verified); this was stale pre-15.3 data + the 15.7 reset. **`encash_pl` still decrements `entitlement`** which has been derived since 15.7 (a read would overwrite it) — pre-existing, out of scope; encashment needs reworking against the derived model if used. `_latest_balance` in late_service now unused (left in place). Container self-heals on rebuild (startup materialize runs). |  **Part A:** new dashboard **Used leaves** card (`UsedLeavesCard`) rendered in a 2-col grid beside the apply/balance card; shows each type's **ULB as used/total** ("SL 3 / 7") + a total, reading the same `useLeaveBalance` shared cache (15.7) keyed by emp_code → invalidates/refetches together on apply (no new fetch). **Part B:** `GET /api/attendance/template` pre-marks approved leaves. The template is **aggregate-count format** (P/A/L/R/C/PL/S/H counts, no per-day cells), so — matching that format, not inventing one — it pre-fills the **C/PL/S count columns** with approved-leave day counts and writes the exact dates into **Remarks** ("APPROVED (do not edit): CL 10-Jun; …") plus a 3-line legend. Window = the **26→25 pay-cycle** (`_attendance_window`, same as the payroll engine/payslip) — deliberately NOT `get_period_dates` (which 14.14 made calendar-month). Approved source = `attendance_daily` rows with att_status cl/sl/pl (written by approval, 15.3) via `_approved_leaves_in_window`. **Part C:** import commit guard — for each row, if uploaded CL/SL/PL count < approved-in-window count, clamp back up to approved + add a warning to the result (`warnings[]`); unchanged pre-marked counts = no-op; blank still skipped (rule 7). Frontend import-result UI lists the kept-approved warnings. **Also removed the import's legacy leave-`used` deduction block** (it double-counted against 15.7's single-source `used`, which is now mutated only at the approval point) — replaced by the clamp+warn guard; dropped the now-unused `LeaveBalance` import. **Acceptance (all ✅):** (1) approve CL 10-Jun → June template C=1 + Remarks "CL 10-Jun"; (2) approve CL 27-Jun (≥26) → JULY template, not June (spec's 28-Jun is a Sunday → leave reflection skips Sundays, so verified with 27-Jun); (3) Used card ULB == balance ULB == payslip ULB (one resolver); (4) unchanged re-upload no-op / no dup, conflict (CL→present) warns + keeps CL; (6) golden UM000001 9349/785/71/0/8493; (7) tsc + next build clean. All verified on disposable US000001 with full cleanup. | **Running Docker container still has OLD code** — rebuild to apply. **Template/import are aggregate-count, not per-day** — so "pre-mark the cell" = count column + Remarks date (no literal per-day cell exists in this format; deviation from the spec's per-day wording, forced by the real format per the "match it, don't invent" instruction). **Window is 26→25** (engine/payslip), not `get_period_dates` (now calendar-month) — the spec's get_period_dates reference is outdated. **Import no longer deducts leave `used`** (approval is the single source, 15.7); the payslip self-heals leave counts from attendance_daily on reprocess regardless. No schema touched → snapshot/seed unchanged. PL options in UI still labelled "Earned" (valid PL label). |  **Revises the 15.3/15.4 grant model: entitlement is now DERIVED + auto-materialized, never hand-set.** **Part A:** `leave_engine.resolve_leave_balance(emp, as_of, db, write_through=True)` — the canonical resolver returning `{cl,sl,pl:{tb,ulb,alb}}`; `tb = completed_leave_years × policy` (precise anniversary date math via `completed_leave_years`, NOT days/365; 0 for workers & before first anniversary), `ulb = stored cumulative used`, `alb = max(tb−ulb, 0)`. Policy from the single editable `leave_policy` table (migration `012_leave_policy.sql`, seed CL10/SL7/PL14 for all entities; model `LeavePolicyConfig` — renamed to avoid clash with the legacy per-entity `LeavePolicy`/`leave_policies`). **Part B (auto-materialize):** (1) on-read write-through — resolve updates the stored entitlement (+ generated balance) and ensures CL/SL/PL rows; (2) FastAPI `lifespan` runs `materialize_all_leave_balances` at startup + a daily asyncio loop (chose asyncio over APScheduler — zero new deps); (3) `ensure_leave_rows` on employee create + DOJ change. `/run-annual-grants` demoted to a manual force-refresh (calls the same write-through). New GET/PUT `/api/leave/policy` (PUT super_admin → re-materializes). **Part C:** rerouted EVERY consumer through resolve — GET /balance (write_through + commit), payslip `_build_response` TB/ULB/ALB block (read-only), late_service highest-bucket availability, and both apply + approve availability/debit paths. `used` still mutated only by real events (approval debit, late-cover). **Part D:** migration reset bogus test used/taken_ytd→0; materialize created missing rows (UM000001 PL) + wrote derived entitlement → UM000001 reads 10/7/14, UP000001 60/42/84. **Part E (frontend):** new shared Zustand store `store/leaveBalance.ts` (`useLeaveBalance` hook + `invalidateLeaveBalance`) — one cache entry per emp_code, `refetchOnWindowFocus`, invalidate on apply; dashboard `LeaveApplyCard` + standalone apply page both use it (removed the two independent drifting fetches); chips now show **available / total** ("CL 8 / 10"). **Acceptance (all ✅):** (1) shared cache + focus refetch (no manual reload); (2) DOJ 1yr+1d → 10/7/14 with NO endpoint call; (3) DOJ 6mo → 0; (4) DOJ 2yr, CL used 3 → TB20/ULB3/ALB17; (5) policy CL10→12 → TB jumps years×2 on next read, no migration; (6) UM000001/UP000001 clean + UM000001 PL row exists; (7) payslip == /balance == late lookup == approval check; (8) golden UM000001 9349/785/71/0/8493; (9) tsc + next build clean, schema re-dumped, deploy seed `00_init.sql` rebuilt. | **Running Docker container still has OLD code** — rebuild to apply (also reload its DB: schema_snapshot/00_init updated). **OPEN (client decision): accrual is UNCAPPED — carry-forward forever (UP000001 already shows 84 PL / 60 CL at 6 yrs).** Many firms cap PL/EL (e.g. 30–45 days); if wanted it's a one-line clamp in `resolve_leave_balance` (`tb = min(years×policy, cap)`). **`year` column on leave_balances is now vestigial** (single cumulative row per bucket; resolve uses the latest-year row, updates in place). **Two consumers invalidate on apply; admin approval elsewhere relies on window-focus refetch** to update the employee's other tab (no cross-session push). Legacy `LeavePolicy`/`leave_policies` (per-entity) + `_get_or_create_balance` now unused but left in place. Daily refresh is an in-process asyncio loop (per worker) — fine (idempotent); revisit if multi-worker/serverless. |  **Config** `LATE_DAYS_PER_ABSENT=3`. **Part A (`011_late_ld.sql`):** payroll_months +`late_days int`, `absent_from_late numeric(5,2)`, `ld numeric(10,2)`, `ld_overridden bool`, `late_absent_overridden bool` (all default 0/false); **GENERATED `total_deduction` rebuilt to `(((((pf_emp+esic_emp)+pt)+loan_emi)+other_deduction)+ld)`** (drop+readd; SQLAlchemy `Computed(...)` string updated to match exactly; v_payslip_summary dropped+recreated since it depends on total_deduction; +ld/late_days/absent_from_late surfaced in the view). **Part B (`services/late_service.py` `compute_late_effects`):** late_days = count 'late' att_status in the 26th-cycle window; absent_from_late = floor(late_days/3); cover absent-equiv from the HIGHEST CL/SL/PL available balance first, spilling to next-highest, decrementing that bucket's `used`; uncovered_days → `ld = round(uncovered*monthly_gross/30, 2)`. **Idempotent** per period via a `LATE_COVER:{emp}:{yyyy-mm}` ledger in leave_accrual_log — recompute available = balance+prior-cover, apply only the delta to `used`, rewrite the ledger (mirrors the loan 'applied' reconcile). Engine calls it (passing `monthly_gross=gross` + any stored override), folds `ld` into the Python total_deduction + net; **uncovered late charged ONCE as LD, NOT also added to /30 LOP** (LOP stays real absent+lwp). **Part C (override):** `POST /api/payroll/late-override {emp_code,year,month,absent_from_late?,ld?,reason}` — super/entity admin, 400 if month locked, reason≥4, sets the override flag(s)+value(s), reconciles leave to the new absent, reprocesses (override-aware) to recompute net, audit `LATE_LD_OVERRIDE` old/new+reason. Frontend: payslips page shows an admin **Late & LD** card (late/absent-equiv/LD) with an **Edit** modal (unlocked only) → `apiLateOverride` → refetch. **Part D (payslip):** LD row (added 15.2) now carries the real value; total_deduction (generated, incl. ld) + net reflect it; added a small "Late days N → M absent-equivalent" note (PDF template + frontend doc) so employees see why. **Part A UI:** attendance summary table + CSV export + import parse + downloadable template gain an **LT (Late)** column (amber); template also fixed `E`→`PL` (15.3 leftover); colSpan bumped. **Acceptance:** (1) 6 late, gross 9000, CL5 → absent 2, cover 2 CL (CL.used 2), ld 0; (2) 6 late, balances 0 → uncovered 2, **ld 600**; (3) 5→1, 3→1, 2→0; (4) override ld=999 sticks across reprocess + audited, **locked→400**; (5) golden UM000001 **9349/785/71/0/8493**, ld 0, total_deduction 856; PDF renders (1.9MB); tsc + next build clean (24 routes); schema re-dumped. All verified on disposable US000001 with full cleanup/restore. | **Running Docker container still has OLD code** — rebuild to apply. **LD vs LOP decision:** uncovered late days are charged ONLY as the LD deduction line — they do NOT also prorate earnings (no double-count); real absences/lwp still drive the /30 LOP. **`late_days` source = attendance_daily 'late' in the 26th window** (authoritative). CSV-import `late_days` is stored/shown but, like other day buckets, **self-heals to the attendance_daily count on reprocess** (so import-only late won't drive LD unless 'late' daily rows exist) — same documented tension as 15.3; biometric/daily 'late' is the real driver. **Override stores target absent/ld + flags; reprocess re-covers leave to the target** — leave cover always follows the (possibly overridden) absent_from_late. Late-cover consumes leave via `used` (tracked in the LATE_COVER ledger, separate from approved-leave `used`). PT/ESIC unaffected. |  **Part A — retire EL (`010_leave_buckets.sql`):** fold EL→PL per (emp,year) in leave_balances (no EL data existed; idempotent), tighten leave_balances leave_type CHECK → {CL,SL,PL}, attendance_daily att_status CHECK 'el'→'pl', delete any non-{CL,SL,PL} leave_policies, rename `payroll_months.days_el`→`days_pl` (drop+recreate v_payslip_summary), audit `RETIRE_EL`. **Part B — flat grant (`leave_engine.py` rewritten):** config `ANNUAL_LEAVE={CL:10,SL:7,PL:14}`; `grant_annual_leave(emp,as_of,db)` — year_index=(as_of−doj)//365, skip if <1 (no balance), idempotent key `ANNUAL_GRANT:{emp}:{idx}` in leave_accrual_log.reason; on each anniversary new entitlement = prior unused (carry-forward) + quota, used reset 0; `run_grants` batches active emps. Removed `run_monthly_accrual`/`calculate_monthly_cl_sl`/`get_months_since_probation` + the 2-1 pattern; probation decoupled from leave (`check_and_end_probation` kept, att_status 'P'→'present' fixed, no longer auto-called); `encash_pl` kept (operates on granted PL). Endpoints: `POST /run-monthly-accrual`→`POST /run-annual-grants` (batch, super/entity admin, optional as_of_date+entity); `GET /balance/{code}` → CL/SL/PL `{tb,ulb,alb}` (+ entitlement/used/balance aliases for existing consumers); accrual-log endpoint kept (now logs grants). EL removed from `create_leave_request` validation + streak calcs. **Part C — leave→attendance (`reflect_leave_on_attendance` + both approve endpoints):** on approve, write each working day (skip Sun) into attendance_daily with att_status cl/sl/pl (source='leave', upsert per (emp,date)) so payroll counts it PAID (not LOP), and bump leave_balances.used+taken_ytd (single point; legacy `/approve/{id}` already bumped, v2 `/request/{id}/approve` now does too); idempotent via approve-once status guard + (emp,date) upsert. The **payroll engine attendance query** changed from calendar-month to the **26th pay-cycle window** [26th prev month .. 25th this month] so day≥26 routes to next month's run and spans split automatically; `_count` adds `days_pl`. **Frontend EL removal:** attendance page (type/CSV header EL→PL/table E→PL col/cell), payslips type days_el→days_pl, dashboard LEAVE_TYPE_COLOR (drop EL), api.ts LeaveBalanceEntry (+tb/ulb/alb). **Acceptance:** grant @ doj+1yr = CL10/SL7/PL14 used0, pre-anniversary skipped, 2nd-yr carryover PL=4+14=18; EL gone (constraints+code+UI, grep clean); leave 24-25→June period / 26-27→July period (boundary split via window); 2 pl + 2 absent → pay_days 28 (PL not LOP); golden 9349/785/71/0/8493; tsc + next build clean (24 routes); schema re-dumped. | **Running Docker container still has OLD code** — rebuild to apply. **Attendance attribution is now 26th-cycle** (a deliberate change from calendar-month counting); EARNINGS proration stays calendar-month /30 (14.14/15.1) — the two periods intentionally differ (salary monthly, attendance cutoff mid-cycle). **leave-year = calendar year of as_of_date** for the leave_balances.year key; apply/approve still look up balance by `from_date.year`, so a leave applied in Jan–May before the DOJ anniversary may not find that leave-year's row (pre-existing anniversary-vs-calendar-year modelling gap, not solved here). **Two approve paths** both now reflect+debit; the legacy attendance-CSV-import leave deduction (13.14) still exists — don't both bulk-import CL/SL/PL days AND approve a request for the same days (would double-count used). `check_and_end_probation` now unused (kept for future wiring). PL options in UI still labelled "Earned"/"Earned Leave" (valid PL label; not relabelled). |  **Part A (payslip PDF `templates/payslip_template.html` + `_build_response`):** removed the **Authorised Signatory** block (+`.sign` CSS) and the **DA** row; EARNINGS now always render **Basic / HRA / Special / CCA / LTA** (CCA + LTA were conditional — now unconditional; kept SPL since it's a gross component), **Other Allowance** still conditional; DEDUCTIONS keep **PF/ESIC/PT/Loan** (ESIC+PT now always shown with value) and add an **LD (Late Deduction)** row when `ld>0`; footer now has **GROSS (Rate)**, Total Earnings, Total Deductions, and **NET PAY** as labelled lines; the old single-line leave balance replaced by a **TB/ULB/ALB × CL\|SL\|PL** grid (`.leave-tbl`) from LeaveBalance (TB=entitlement, ULB=used, ALB=balance; missing bucket → 0/0/0). `_build_response` adds `ld`=0 (wired in 15.4) + structured `leave` block; kept `leave_balances` for back-compat. **Part B (`payslips/page.tsx` PayslipDocument, inline-styled to mirror the PDF):** same changes — removed signatory + DA, always-show CCA+LTA, ESIC/PT always, LD row, GROSS+NET footer lines, TB/ULB/ALB grid; PayslipData type gains `ld` + `leave:{cl,sl,pl}` (each `{tb,ulb,alb}`); dropped the now-unused `leaveEntries`. **Part C (`statutory/page.tsx` calculator):** added **LTA** + **Other allowance** inputs; `calcPayroll` gross=basic+hra+spl+cca+lta, net=gross−PF−ESIC−PT+other; GROSS + NET both shown prominently & labelled; PF line '12% of basic, cap ₹1,800' + employer '13% of basic, cap ₹2,340' + note 'cap reached when basic ≥ ₹15,000'; per-day helper '1 day = gross ÷ 30'. **Part D (employee ADD/EDIT/detail):** removed **PF-applicable** + **PT-applicable** toggles from UI **and** from create/update payloads (add form: dropped from FormState/EMPTY/payload/UI; detail: dropped the 2 InfoRows + added a `skip` set so pf/pt/esic_applicable + monthly_gross are never PUT); added **LTA (leave_travel)** as a labelled field in add + edit (+ live-gross calc + numFields); IncrementModal already carried LTA from 15.1. **Acceptance:** golden UM000001 Apr 2026 = **9349/785/71/0/8493**; PDF renders (WeasyPrint 1.9MB, valid %PDF) — HTML asserts no 'Authorised Signatory'/'D.A.', GROSS+NET present, CCA+LTA+LEAVE-TRAV present, TB/ULB/ALB present; leave block CL 12/0/12 SL 6/0/6 PL 0/0/0; calculator basic 20000 → PF 1800 (capped); add form has no DA/PF-applicable/PT-applicable, has LTA; tsc + next build clean (24 routes). | **Running Docker container still has OLD code** — rebuild to apply. **Deductions ESIC/PT now always show a row** (value 0 when exempt/NIL) instead of being hidden — clearer but a visible behaviour change. **`leave_balances` (flat dict) kept in the payload** for any other consumer though the payslip now uses the structured `leave` block. **PF cap note assumes the standard ₹15,000 PF wage ceiling** (1800 = 12% × 15000); caps themselves remain the configured company caps (₹1,800 / ₹2,340). LD is display-only/0 until 15.4 wires late-deduction values. |  **DA eliminated; pay unchanged.** **Migration (`009_remove_da.sql`, one txn; backup `backup_pre_15_1_da.sql`):** dropped the 2 dependent views → `UPDATE salary_structures/employees SET basic = basic + da` (fold) → `ALTER ... DROP COLUMN da` on employees + salary_structures + payroll_months (payroll_months snapshots NOT rewritten — column just dropped; `total_deduction` GENERATED expr never referenced da, no rebuild) → recreated `v_employee_full`/`v_payslip_summary` da-free → audit `REMOVE_DA`. **Backend:** removed `da` from ORM (Employee/SalaryStructure/PayrollMonth), all schemas, payroll_engine, payslip `_build_response`, salary_resolver-side increment_service `_COMPONENTS`/`_STATUTORY`, employees.py + admin.py field-sets/CSV/gross calls, import_service alias+gross+ctor, **attendance.py CSV-import ctor**. **Gross = basic+hra+spl+cca+leave_travel**; **PF base = basic only** (`min(round(0.12*basic),1800)` / `min(round(0.13*basic),2340)`); ESIC + other_allowance unchanged. **/30 proration (`PART C`):** new `config.py` `PER_DAY_DIVISOR=30`, `CYCLE_CUTOFF_DAY=26`; in `process_payroll_month` `LOP_days=days_a+days_lwp`, `payable_factor=max(0,30−LOP)/30`, earnings prorate by factor (other_allowance always full), deductions on full statutory gross, `net=total_earnings−total_deduction`, `pay_days=30−LOP` stored; `_build_response` factor → `pay_days/30` (capped 1.0); removed old `pay_days/total_days` + `/26` fallback. **Bug fixed:** `_count` att_status used uppercase `'A'/'LWP'` but the DB CHECK + all writers use lowercase (`present/absent/lwp/…`) → LOP was structurally always 0; now lowercase so proration actually fires. **Frontend:** removed da from api.ts `SalaryStructureRow`, SalaryHistoryTable (col), IncrementModal (field+gross), payslips type+D.A. row, employees add/[emp_code]/bulk-import field arrays, statutory calculator (input+PF formula `basic` only + card copy). Payslip PDF template D.A. row removed. **Acceptance:** golden UM000001 Apr 2026 = **9349/785/71/0/8493**, basic 6542, no da; synthetic 3-LOP test on gross 9000 → earnings 8100 / 900 reduction (reverted, no leak); grep zero `da` backend+frontend; tsc + next build clean (24 routes); schema_snapshot re-dumped. | **Running Docker container still has OLD code** — rebuild to apply. **CSV attendance import vs reprocess (pre-existing):** the CSV import writes `days_a` etc. directly to payroll_months but `process_payroll_month` re-derives them from `attendance_daily` (now lowercase-correct) and overwrites on any payslip view of a non-locked row — so CSV-only attendance with no daily rows still self-zeros on reprocess (unchanged from before; biometric/punch path now works for LOP). **leave_engine.py:34** still uses uppercase `["P"]` (out of scope — leave accrual, not payroll). Locked/processed payroll_months keep their old snapshots (da data lost from those rows, but stored gross/net intact). |
| 14.15 | Nav cleanup: remove "More" + "Master data" | ✅ Done | Removed the **"More"** item from the mobile **BottomNav** (`BottomNav.tsx`, + dropped the now-unused `MoreHorizontal` import) and the **"Master data"** entry from both the **Sidebar** HR-Admin group (`Sidebar.tsx`) and the **dashboard quick-action tile** (`dashboard/page.tsx`). `BookOpen` (Sidebar→Leave) and `FileText` (dashboard) kept (still used). No master-data/More nav refs remain; `tsc` clean. Committed+pushed **c21f73a**. | **Nav-only:** the `/dashboard/master-data` page and `/api/admin/master-data` endpoint still EXIST — just unlinked from navigation. Say so if you want the page/route + endpoint removed entirely. `/dashboard/more` route may now be orphaned (no nav links to it). |
| 14.14 | Payroll period → calendar month; increment effective from 1st | ✅ Done | **Client clarified the real rule** (was built wrong): salary period is the **calendar month (1st→last day)**, not the 26th→25th cycle; an increment granted mid-month leaves that month at the OLD rate and the new rate applies from the **1st of the next month** (one month = one rate, no mid-month proration). **Backend:** `period_calculator.get_period_dates` → `date(y,m,1)` … `calendar.monthrange` last day (was 26th prev→25th); `increment_service` date guard `day != 26` → **`day != 1`** + error "Increment must be effective from the 1st of the month"; docstrings/comments in `salary_resolver`, `payroll_engine`, `api/employees.py`, `loan_service`. **Frontend:** `IncrementModal.tsx` (`is26th`→`is1st`, validation/helper/error text), `about/page.tsx` blurb. **NOT touched (different use of 26):** `payroll_engine` `pay_days/26` proration fallback + `leave_engine`/`leave.py` `basic/26` PL-encashment daily rate (standard 26-working-days divisor, unrelated to period boundary). **Verified:** `get_period_dates(2026,4)`=`(2026-04-01,2026-04-30)`; **golden UM000001 Apr 2026 = 9349/785/71/0/8493** (unchanged — no attendance → full month); increment guard 1st=accept / 26th+15th=reject (tested via rollback, golden fixture intact, 1 active structure); frontend `tsc` clean. | Period dates + working-days now differ on every FUTURE payslip (proration for months WITH attendance shifts to calendar-month basis — correct per new rule). Already-locked/processed `payroll_months` keep their old 26–25 snapshots (frozen); only new processing uses calendar months. **Running Docker container still has OLD code** — `docker compose -f docker-compose.prod.yml up -d --build` to apply. Committed+pushed. |
| 14.13 | Bugfix: browser login 404 in Docker (double /api) | ✅ Done | Browser login on `:8080` returned **"not found"** while the API itself worked (curl `POST /api/auth/login` via Caddy → **200** + tokens). **Root cause:** the frontend builds URLs as `baseURL + path`; every code path already starts with `/api` (e.g. `api.post("/api/auth/login")`), but the Docker build arg set `NEXT_PUBLIC_API_URL=/api` → combined to **`/api/api/auth/login`** → 404. (Dev worked because its `NEXT_PUBLIC_API_URL` is the bare origin `http://localhost:8000`.) **Fix (3 files):** build arg `NEXT_PUBLIC_API_URL` `/api`→**empty** (same-origin) in `docker-compose.prod.yml` + `frontend/Dockerfile`; `frontend/src/lib/api.ts` `\|\| "http://localhost:8000"` → **`?? "..."`** so the empty (same-origin) base is honored instead of falling back. Rebuilt frontend (`up -d --build frontend`); confirmed baked bundle now has `/api/auth/login` (single `/api`), no `/api/api`, no `localhost:8000`. **User CONFIRMED browser login works** (UP000001/Admin@2026) after hard-refresh. Committed+pushed **7e5a5b3**. | **Empty base = same-origin is also correct for the VPS** (one image works on :8080 and :80, any IP, no per-host rebuild). `api.ts` change is app-code but dev-safe (dev sets its own origin in `.env.local`). **Browser cache:** users must HARD-REFRESH after a frontend rebuild or the old bundle (with the bug) keeps running. **Still unverified:** payslip **PDF download** in-container (WeasyPrint+fonts). **VPS deploy still pending** (clone/rsync + 2 gitignored files + ufw 8080 + deploy.sh; need IP/user/OS/sudo). |
| 14.12 | LOCAL Docker stack validated end-to-end | ✅ Done | On a network **without** the office proxy, the full `docker compose -f docker-compose.prod.yml build` **succeeded** (backend `pip install` + frontend `npm ci`/`next build`). Images: backend 718MB / frontend 296MB. `up -d` → **all 4 containers healthy** (postgres + backend + frontend + caddy on host **:8080**). Postgres first-boot loaded `00_init.sql` → **11 employees / 6 loans**. Verified through Caddy: `GET /api/health`→`{"status":"ok","db":"connected"}`, `/`→307→`/login`, `/login`→**200**. **Golden UM000001 Apr 2026 in the containerized DB = 9349/785/71/0/8493** ✓. **Bugs fixed during bring-up (all committed+pushed, `995719d`):** (1) `apt-get` `libgdk-pixbuf2.0-0` "no installation candidate" — package renamed in **Debian trixie** (python:3.12-slim base); **removed it** (WeasyPrint 69 renders rasters via Pillow, doesn't need it). (2) DB init `ERROR: unrecognized configuration parameter "transaction_timeout"` — dump is from **PG18**, container was **postgres:16** → bumped to **postgres:18**. (3) PG18 image rejects old data path → changed volume mount `/var/lib/postgresql/data`→`/var/lib/postgresql`. Also fixed `DATABASE_URL` password (`__2399__`→`2399`) to match `POSTGRES_PASSWORD`. | **WeasyPrint PDF not yet browser-verified in-container** (health/DB/frontend confirmed; user to download a payslip PDF to exercise WeasyPrint+fonts). **Local build only works off the office TLS-proxy network** (see 14.11) — VPS expected clean. **VPS deploy still pending:** clone repo (gh auth on VPS) or rsync; copy the 2 gitignored files up (`backend/sql/init/00_init.sql` + `backend/.env.production`); `ufw allow 8080`; `./deploy.sh`; test `http://VPS_IP:8080/`. Need from user: VPS IP / SSH user / OS / sudo. For real go-live (later): strong `POSTGRES_PASSWORD`/`SECRET_KEY` (keep `ENCRYPTION_KEY` = `change-me-32-bytes-base64-encoded=` so dump decrypts), then switch caddy 8080→80 after stopping old system. |
| 14.11 | Deploy configs + git push (local build blocked by proxy) | ⚠️ Partial | **Created the missing deploy files** (the earlier `docker-compose.prod.yml`/`Caddyfile`/`deploy.sh` existed; these did not): `frontend/Dockerfile` (multi-stage, Node 22, Next **standalone**, `NEXT_PUBLIC_API_URL=/api` build-arg), `next.config.ts` → `output:'standalone'`, `frontend/.dockerignore` + `backend/.dockerignore`, root `.env` (compose Postgres vars), `backend/.env.production` (DB host=`postgres` service; **ENCRYPTION_KEY mirrors local** so dump decrypts). **Added `/api/health` alias** in `app/main.py` (route was only `/health`, which Caddy can't reach via `/api/*` → deploy.sh/Caddy health check needs `/api/health`). `docker compose config` validates. **WeasyPrint already at 69.0** (no bump needed). **Git:** project was NOT a repo; removed nested boilerplate `frontend/.git`, wrote root `.gitignore` + fixed `backend/.gitignore` negation, committed 124 files, created **PRIVATE** repo **github.com/martis444/UDYOGI_HRMS** (`main`) and pushed. Security-swept: no `.env*`/`00_init.sql`/`node_modules`/`.next`/`venv` tracked; env **templates** (`.env.example`, `backend/.env.production.template`) included. | **LOCAL build BLOCKED:** office network has a **TLS-inspecting MITM proxy** (`192.168.0.254:8090`, via Docker Desktop `http.docker.internal:3128`) — HTTP apt → 403, HTTPS apt → cert-verify-failed. Image PULLS work (allowlisted), in-build apt/npm don't. **Not a code bug** — builds fine on clean internet. **Decision: build on the VPS** (clean outbound). **TODO (VPS, per DEPLOY_GUIDE_TEST.md):** `git clone` the private repo on the VPS (needs `gh auth`/PAT/deploy-key) OR rsync; then copy the 2 gitignored files up separately — `backend/sql/init/00_init.sql` + `backend/.env.production`; open firewall 8080; `./deploy.sh`; test `http://VPS_IP:8080/`. Still need from user: VPS IP / SSH user / OS / sudo. Commit identity = martis444/abhibb36@gmail.com. |
| 14.10 | Deploy test-DB seed dump | ✅ Done | Generated **`backend/sql/init/00_init.sql`** — a full schema+data `pg_dump` of local `udyogi_hrms` (`--no-owner --no-privileges`) to seed the Docker test database. Verified: 179KB/3146 lines, **25 CREATE TABLE**, **25 COPY** data blocks, `pgcrypto` extension present, **0** `CREATE DATABASE`/`\connect` (so it loads cleanly into the container's `POSTGRES_DB` via `/docker-entrypoint-initdb.d` on first boot), 11 employees (UM000001 golden + UP000001–008 figures + UM000002/US000001 test) + 6 dummy loans. This is the "test database" for the port-8080 test deploy (per DEPLOY_GUIDE_TEST.md Phase 2). | **Encryption key must match:** dump stores aadhaar/bank as pgcrypto ciphertext encrypted with the LOCAL key — container backend `.env.production` ENCRYPTION_KEY must equal local `backend/.env` or decryption errors. Init runs **only on first boot** (empty volume); reload = `down -v` then `up -d`. Contains test rows + dummy loans (fine for test; clean before real go-live). **Not in git** (no repo) — when git is set up, `backend/sql/init/` + `backend/.env.production` MUST be gitignored (encrypted PII). **Still TODO before deploy:** create docker-compose.prod.yml / Caddyfile / deploy.sh / frontend Dockerfile / next.config standalone / .env.production / WeasyPrint pin bump — all per DEPLOY_GUIDE_TEST.md (none exist yet). |
| 14.9 | Loans tab: polish + banner readability bugfix | ✅ Done | Follow-up `/impeccable polish` on `src/app/dashboard/loans/page.tsx`. **Polish:** added `.press` click-feedback to the 3 row-action buttons (Manage/Edit/Close) to match app-wide button behavior; swapped the Close icon **`Ban`→`Archive`** with hover recolored **red→amber** (Ban read as "forbidden" + red implied delete, but closing just settles a loan — outstanding stays on record); removed unused `Ban` import. **Bugfix (user-reported "not readable"):** the info banner used near-black text `#1A1A1A` on a `bg-[#2563EB]/[0.07]` (7%) panel — but the Loans page sits on the **dark** dashboard chrome, so 7%-blue-over-dark stayed dark → black-on-dark, invisible (the table reads fine only because it's inside a white GlassCard; the banner was a bare div on the dark area). Fixed to a dark-theme banner: `text-white/85`, Info icon `#93C5FD` (light blue), "Manage months" `text-white font-semibold`, bg `/15` + border `/30`; wrapped copy in a `<span>` for clean icon alignment. `tsc` + `next build` clean (24 routes); detector `[]`. | Watch for the same dark-text-on-translucent-tint pattern on any other dashboard page that has a **bare (non-GlassCard) notice/banner** on the dark chrome — offered to grep for others. Modal commit buttons (Create/Save/Confirm) still lack `.press` (left for consistency-only follow-up). Empty-state still generic ("No loans match" doesn't distinguish no-loans-yet vs no-filter-match) — `onboard` territory. |
| 14.8 | Login page: impeccable polish | ✅ Done | Ran `/impeccable polish` on `src/app/login/page.tsx` (grounded in design tokens; **Enter-to-submit left owned by `LiquidGlassScene` keydown — no competing `<form>` added**). **Removed dead controls (user-approved):** the no-op "Remember me" checkbox (tokens always persist to localStorage) + the misleading "Forgot password?" link (`href="#"` — this system resets passwords admin-side only via reset-password temp-password flow). **A11y:** `htmlFor`/`id` label↔input associations (Employee ID + Password), error message now `role="alert"`, show/hide toggle `aria-label`+`aria-pressed`, Sign In button now shows a **keyboard-focus** state (mirrors hover shadow via onFocus/onBlur). **Flow/craft:** `autoFocus` on Employee ID; restored vertical rhythm before the button after the row removal and pulled the button entrance into the shorter stagger (`0.5s`). Liquid-glass scene + Saira/IBM Plex fonts untouched (committed brand). `tsc` + `next build` clean (24 routes); detector `[]`. | `change-password/page.tsx` shares the same scene and likely the same label-association gap — **not touched** (out of scope); apply matching a11y fixes there if wanted. No PRODUCT.md written (project still has none). |
| 14.7 | Loans tab: impeccable critique + harden/polish | ✅ Done | Ran `/impeccable critique` on `/dashboard/loans` (one-off; project has **no PRODUCT.md** so no init/persistence — user chose skip). **Score 28/40 "Good"; detector `[]` clean; no browser overlay (no automation this env).** Top findings: modals lacked Esc/scrim-dismiss/focus-trap/aria (P1), employee dropdown clipped inside modal `overflow-y-auto` (P2), filter search broke the shared input vocabulary (P2), icon-only actions + silent toast (P2). **Applied harden + scoped polish (user picked Top-3) — all in `src/app/dashboard/loans/page.tsx`:** new shared `useDialog` hook → `ModalShell`+`ConfirmModal` get **Escape close, backdrop mousedown-dismiss (panel stops propagation), focus trap + focus-return, `role=dialog`/`aria-modal`/`aria-labelledby`**; employee picker now renders via **`createPortal` to document.body, `position:fixed`** anchored to input (re-tracked on scroll/resize, z-[60]) — un-clippable; **a11y:** aria-labels on row-action/close/override icons + `focus-visible` rings, toast now `role=status aria-live=polite`; **polish:** filter search restyled onto the shared `INPUT` constant, "N shown" count `white/40→white/60`. `tsc` + `next build` clean; detector re-scan clean. | Behavior change: dialogs focus first field on open → Add-Loan modal now auto-opens the employee dropdown (benign "pick employee first" cue; easy to revert by focusing the panel instead). **Deferred** (out of chosen scope): empty-state onboarding (true-empty vs filtered-empty), `Ban`→archive icon, `.press` consistency on row actions, app-wide subtitle `white/50` contrast. No PRODUCT.md/DESIGN.md written — run `/impeccable init` if persistent design context is wanted. |
| 14.6 | Post-14.5 final checks + UI fixes | ✅ Done | **Pre-deploy final checks (after 14.1–14.5):** golden test re-run through the LIVE engine (`compute_payroll('UM000001',2026,4)`) → **9349 / 785 / 71 / 0 / 8493** ✓ — 14.1 remap (UM000001 on NOIDA, pt_state_code NIL) and 14.3 loans (UM000001 loan-free) did NOT break it; `npx tsc --noEmit` exit 0; `npx next build` clean (24 routes, eslint advisories did not block); **`backend/sql/schema_snapshot.sql` re-dumped** (now 25 tables incl. loans + loan_emi_schedule). **Bug fix:** `LiquidGlassScene.tsx` `InvalidStateError` from `a.finish()` — the 13.14d `playState` guard didn't cover INFINITE (looping) animations, which always throw on finish(); now only fast-forwards animations with a FINITE `getComputedTiming().endTime`, wrapped in try/catch. **Credits/copyright placement (all from `APP_META`, dynamic year):** login page → "Developed by {developer}" + "© {year} {holder}" under the footer; About page → "Developed by {developer}" in identity card; Payslips tab → "© {year} {holder}" centered footer (outside `#payslip-print-area`, screen-only). **Loans tab:** removed the 3 summary cards (Total outstanding / Active loans / This-month EMI) + their now-unused memos. All `tsc` clean; impeccable design hook clean on every edit. | **Test-row cleanup DEFERRED by user** — nothing deleted. Still pending before deploy: 6 dummy loans (`DELETE FROM loans;`), test employees UM000002 + US000001 (+ 6 payroll_months, 2 salary_structures, 2 users; FKs are NO ACTION → delete children first). UP000005–008 keep placeholder DOJ 2000-01-01 / mobile 0000000000 for HR. Also still open: `requirements.txt` pins WeasyPrint 62.3 (needs ≥69.0 + pango); 3 manual browser sign-off checks for Session 14. |
| DB | Database setup | ✅ Done | PostgreSQL running in Docker. 001_init_schema.sql + 002_seed_data.sql applied. All 18 tables created. Entities, locations, shifts, grades, departments, statutory_config seeded. | — |
| 0 | Project scaffold | ✅ Done | Folder structure created. requirements.txt installed. Empty boilerplate files created. Dockerfile created. uvicorn starts without errors. | — |
| 1 | Core config + DB + security | ✅ Done | `config.py` (SettingsConfigDict), `db.py` (connect probe on import), `security.py` (argon2 + JWT with sub/role/entity_id/exp), `main.py` (DB-aware /health). `.env` created. `curl /health` → `{"status":"ok","db":"connected"}` ✓ | — |
| 2 | SQLAlchemy models | ✅ Done | 17 ORM classes in `models/employee.py` mapping all DB tables. `models/__init__.py` exports all. `db.py` imports models so Base is complete. Smoke-test `from app.models.employee import Employee` passes. All 17 tables and correct column counts verified against live DB. | No SQL files on disk — schema introspected directly from live PostgreSQL. |
| 3 | Auth API | ✅ Done | `schemas/auth.py` (4 schemas). `api/auth.py`: POST /api/auth/login (force_reset flow + full TokenResponse), POST /api/auth/refresh (new access_token only), POST /api/auth/change-password (8-char+digit validation, is_first_login reset). `core/dependencies.py`: `get_current_user` (returns User ORM), `require_role()` factory. `scripts/create_superadmin.py` bootstraps UP000001 hash. All 3 test steps pass. Audit log timestamps confirmed. | `AuditLog.ts` needed `server_default=sa_text("now()")` — without it SQLAlchemy sent explicit NULL overriding DB default. |
| 4 | Employee API (CRUD) | ✅ Done | `schemas/employee.py` (EmployeeCreate/Update/Response/ListItem/ListResponse). `services/import_service.py`: `generate_emp_code()` with SELECT FOR UPDATE. `api/employees.py`: GET /employees (list+filters+entity-scope), GET /employees/{code} (decrypt+mask aadhaar/bank_acc), POST /employees (auto-code, pgcrypto encrypt, esic auto-compute, user account create), PUT /{code} (partial, emp_code immutable→400, esic recompute), DELETE /{code} (soft-delete status=inactive), GET /next-code. Audit log on all writes. Registered in main.py. Acceptance test passed: POST UPPL → UP000002. | `model_dump(mode='json')` for audit log (date→str); separate native `model_dump()` for ORM sets (Date/Decimal types must be preserved). |
| 5 | Bulk import | ✅ Done | `import_service.py`: `parse_upload_file` (CSV/XLSX, 40-col alias map), `validate_import_rows` (8 checks, pre-loads entity/location/code sets), `commit_import` (single transaction, flush-per-row for sequential code gen). `api/employees.py`: `POST /bulk-import/validate` (dry run), `POST /bulk-import/commit` (entity-scoped), `GET /export` (CSV download, masked aadhaar, no bank_acc). `employee_upload_template.csv` test file added. All 4 acceptance steps pass. | DB CHECK constraint requires gender in lowercase (`male/female/other`); normalised in `commit_import`. |
| 6 | Payroll engine + PDF | ✅ Done | `services/pt_resolver.py`: `get_pt_amount()` queries statutory_config with effective-date filter + gender priority. `services/payroll_engine.py`: `compute_payroll()` (pure computation), `process_payroll_month()` (upsert payroll_months + audit log, respects locked status, fetches attendance_daily). `services/pdf_generator.py`: `num_to_words()` (Indian format), `generate_pdf()` (Jinja2 + WeasyPrint lazy import). `api/payslip.py`: GET /data, GET /pdf, POST /process-month. `templates/payslip_template.html`: A4 payslip layout per spec. Registered in main.py. Validation test passes: UM000001 Apr 2026 → Gross=9349, PF=785, ESIC=71, PT=0, Net=8493 ✓ | WeasyPrint requires pango/cairo system libs — lazy import so local dev doesn't crash. PDF works in Docker. UM000001 seed salary corrected (was 8500, now 9349). |
| 7 | Biometric push receiver | ✅ Done | `api/biometric.py`: GET /iclock/cdata (heartbeat), POST /iclock/cdata (ADMS punch ingestion), POST /api/biometric/mapping (upsert with audit log), GET /api/biometric/devices. `services/attendance_processor.py`: `process_raw_punches(date, db)` (first_in/last_out, hours, OT, att_status upsert). Two routers registered in main.py. All 4 acceptance tests pass. | eSSL devices prefix each data line with "ATTLOG " — parser strips it before parsing key=value fields. punch_type fits String(5): IN/OUT/OTIN/OTOUT/UNK. |
| 8 | Attendance CSV import | ✅ Done | `services/import_service.py`: added `_safe_int`, `_safe_float`, `parse_attendance_csv()` (legacy HRMS Code → emp_code via legacy_code lookup + direct fallback). `api/attendance.py`: POST /import/validate (dry-run, unmatched/warnings buckets), POST /import/commit (upsert payroll_months attendance columns, compute salary for new rows), GET /monthly-summary, GET /daily. Router registered in main.py. `attendance_template.csv` test file added. All 4 acceptance tests pass. **Bonus fix:** `payroll_months.total_deduction` is a `GENERATED ALWAYS AS` column in PostgreSQL — marked as `Computed(...)` in SQLAlchemy model; removed from all PayrollMonth INSERT constructor calls in payroll_engine.py and attendance.py. | `total_deduction` was never exercised on a fresh INSERT until this session — the Session 6 test hit the UPDATE path (row already existed from seed). Fix applies to both payroll engine and attendance import. |
| 9 | Admin APIs | ✅ Done | `api/admin.py`: GET /master-data (full employee data, entity-scoped, aadhaar masked), GET /master-data/export (CSV, aadhaar last-4, audit on export), POST /column-update/template (pre-filled CSV for chosen columns), POST /column-update/validate (dry-run, blank=skip, type-check, old/new diff), POST /column-update/commit (single transaction, ESIC recompute on salary change, per-employee audit), POST /reset-password (Udyogi@XXXX temp pw, is_first_login=True, returned once), GET /audit-log (newest-first, filterable), GET /statutory (grouped by state_code), PUT /statutory/{id} (super_admin only, audit log). Router registered in main.py. All 3 acceptance tests pass. | — |
| 10 | Frontend: Auth + Layout | ✅ Done | Next.js 16 + Tailwind v4 scaffold. Udyogi design system tokens in globals.css @theme. `src/lib/api.ts` (axios + token interceptor + refresh + all API functions), `src/lib/auth.ts` (useAuth hook, role helpers, getStoredUser), `src/store/entity.ts` (Zustand entity context). `src/proxy.ts` (route guard — public paths + auth_session cookie check). Root layout with Inter font. Login page (dark #1A1A1A background, red radial glows, glass card, logo). Change-password page (same dark style). Sidebar (dark glass, nav groups, red active state, role-filtered admin items). Header (dark glass, entity tabs with red underline). BottomNav (dark glass, mobile only). Dashboard layout (auth guard + sidebar + header + BottomNav + mobile drawer). Dashboard page (KPI cards, entity headcount bar chart via recharts, quick actions, notices). Root `/` redirects to `/login`. All routes verified: root→307, /login→200, /dashboard→307 (no auth). Zero TypeScript errors. Dev server starts clean. | Next.js 16 uses `proxy.ts` not `middleware.ts` (named export `proxy`, not `middleware`). Tailwind v4 has no tailwind.config.ts — custom colors go in `@theme` block in globals.css as `--color-*` variables. Place `/public/udyogi-logo.png` for full logo; LogoFallback SVG used as placeholder. |
| 11 | Frontend: Employee pages | ✅ Done | `GET /api/admin/form-options` endpoint added to admin.py (Entity+Shift models imported). `api.ts` extended with: apiGetFormOptions, apiGetNextEmpCode, apiCreateEmployee, apiUpdateEmployee, apiDeleteEmployee, apiBulkImportValidate, apiBulkImportCommit, apiDownloadEmployeeExport, apiDownloadPayslipPdf. Four pages built with Udyogi Design System (bone bg, white glass cards, red primary buttons): `/dashboard/employees` (filterable table, entity tab filter, pagination, CSV export), `/dashboard/employees/add` (5-section collapsible form, live gross calc, auto emp_code preview), `/dashboard/employees/bulk-import` (4-step wizard: download template → upload → validate → commit), `/dashboard/employees/[emp_code]` (read-only sections + inline edit mode + payslip history month picker + danger zone). Zero TypeScript errors. | Payslip history uses GET /api/payslip/data per period (no list endpoint exists). Employee detail edit intentionally skips entity_id change (immutable per business logic). |
| 12 | Frontend: Payslip + Statutory | ✅ Done | `src/app/dashboard/payslips/page.tsx`: month-tab selector (last 12 months, red active), admin employee picker with dropdown search, quick-stats bar (gross/deductions/net pay), print-accurate white payslip document card mirroring PDF layout exactly (Udyogi logo, all earnings/deductions/attendance columns), Download PDF + Print buttons. `src/app/dashboard/statutory/page.tsx`: PF card (12%/13% rates, ₹1800/₹2340 caps), ESIC card (0.75%/3.25%, ₹21000 ceiling, ceil rounding note), PT location→state mapping table (9 locations), collapsible PT slab cards per state (WB/MH/GJ/NIL), live salary calculator with all 5 components + location/gender/month params, result panel with red Net Pay accent + employer CTC breakdown. Zero TypeScript errors. | Route is `/dashboard/payslips` (plural) matching existing sidebar nav. Payslip document uses inline styles (not Tailwind) to exactly mirror the WeasyPrint PDF template. Calculator replicates `pt_resolver.py` logic client-side (effective-date filter + gender priority). |
| 13 | Frontend: Admin tools | ✅ Done | Three pages built with Udyogi Design System. `src/app/dashboard/admin/audit-log/page.tsx`: filter bar (user code, action dropdown, table dropdown, from/to date, clear-all ×), paginated table (50/page, newest first), action color-coded badges (CREATE=green, UPDATE/COLUMN_UPDATE=amber, DELETE/RESET_PASSWORD=red, LOGIN=blue, EXPORT=gray, IMPORT=purple), row click expands inline JSON diff (old value struck-through muted → new value red). `src/app/dashboard/admin/password-vault/page.tsx`: amber warning banner, debounced employee search dropdown, selected-employee dismissible card, reset → modal with mono temp password + red copy button (flips ✓ green on copy), "how it works" steps card. `src/app/dashboard/admin/column-update/page.tsx`: 4-step wizard with red active step indicator — Step 1 entity selector + column checkboxes grouped by Profile/Salary/Banking with group-toggle + locked columns grayed with lock icon; Step 2 template CSV download (gates Step 3); Step 3 drag-and-drop CSV upload → validate endpoint → summary (employees/changes/errors), scrollable error list; Step 4 diff table (emp_code, field, ~~old~~, new red-tinted) → commit with success state. `api.ts` extended with apiResetPassword, apiColumnUpdateTemplate, apiColumnUpdateValidate, apiColumnUpdateCommit. Sidebar updated: audit log URL corrected to `/dashboard/admin/audit-log`, Column update + Password vault added to HR Admin. Zero TypeScript errors. | — |
| 13.5 | Local integration testing + bug fix | ✅ Done | Systematically tested all backend APIs (Blocks 0–5) and frontend flows (Blocks 6.1–6.7). Bugs fixed: (1) Safari cookie drop — added `SameSite=Lax` to both cookie writes in `api.ts`; (2) Recharts -1 dimensions on Safari — added `minWidth={0}` to `ResponsiveContainer`; (3) Logo "not a valid image" — extended proxy.ts matcher to exclude static file extensions + added `unoptimized` to all `<Image>` components serving `/public`; (4) Payslip page infinite re-render — wrapped `buildMonthOptions(12)` in `useMemo`; (5) PDF download Network Error (WeasyPrint) — upgraded WeasyPrint 62.3→69.0 + `brew install pango` (Python 3.14 fix); (6) PDF download Network Error (Axios blob) — replaced Axios blob call with native `fetch` in `apiDownloadPayslipPdf`; (7) PDF shows placeholder "U" box — base64-embedded logo in `pdf_generator.py`, updated `payslip_template.html` to use `<img>`; (8) Dashboard entity tabs not filtering — replaced hardcoded headcount data with live API calls; (9) Chart names showing "Plastics/Agritech" — fixed ENTITY_META to use actual IDs (UPPL/USAPL/UAPL/UMPL); (10) Add employee 422 silent failure — improved error handler to show Pydantic field-level errors; (11) Payslip employee search dropdown hidden under stats card — added `relative z-10` to controls GlassCard. | Block 5 biometric punch requires `&table=ATTLOG` query param or endpoint silently no-ops. Block 3 ProcessMonthBody requires `entity_id` field. Block 2 location_id is a string ("kol"), not integer. WeasyPrint 69.0 + pango required in production image — `requirements.txt` still shows 62.3 (update before Session 14). |
| 13.6 | Frontend integration testing (Blocks 6.8–6.12) | ✅ Done | Tested and verified all remaining frontend flows. Blocks 6.8 Payslip ✅, 6.9 Statutory ✅, 6.10 Password Vault ✅, 6.11 Column Update ✅, 6.12 Audit Log ✅. Bugs fixed: (1) Payslip logo — switched from Next.js `<Image>` to plain `<img>` tag (eliminates width/height aspect-ratio warning, matches payslip inline-style approach); (2) Month picker — replaced hardcoded 6-tab scroll with year dropdown (2023→current) + 4×6 month grid; future months disabled; supports any year/month for historical payslip lookup. `udyogi-logo.png` confirmed present in `frontend/public/`. | — |
| 13.7 | Block 7 deviation checks | ✅ Done | Verified all known deviations: 7.1 PayrollMonth INSERT — no 500, total_deduction computed by DB ✓; 7.2 AuditLog ts — real timestamps not NULL ✓; 7.3 Gender constraint — fixed: added `field_validator("gender","marital_status","status", mode="before")` with `.lower()` normalisation to both EmployeeCreate and EmployeeUpdate schemas (previously only bulk import normalised); 7.4 ESIC ceiling — esic_applicable=False for gross=23000 ✓; 7.5 Tailwind brand colors — red rendering correctly ✓; 7.6 Proxy route guard — incognito /dashboard redirects to /login ✓. | super_admin password reset during this session (create_superadmin.py → Udyogi@2026 → changed back to Admin@2026). |
| 13.6b | Schema additions: LTA, Other Allowance, Staff/Worker, Leave engine | ✅ Done | Parts A–H complete. DB migrations run (ALTER employees + payroll_months, CREATE employee_categories/leave_policies/leave_accrual_log, ALTER leave_balances). SQLAlchemy models updated (Employee 6 new cols, PayrollMonth 2 new cols, 3 new model classes, LeaveBalance 3 new cols). Schemas updated (EmployeeCreate/Update/Response with leave_travel, other_allowance, category, probation_days, probation_end_date, is_on_probation). Gross calc updated everywhere (basic+hra+da+spl+cca+leave_travel). other_allowance excluded from statutory gross/ESIC/PF but added to net_pay after deductions. All 5 acceptance tests pass. | leave_policies seeded with UNIQUE(entity_id,category,leave_type) constraint added — required for ON CONFLICT DO NOTHING to work. EmployeeUpdate uses Optional[...]=None (not 0.0) for new salary fields to preserve blank=skip behaviour. payslip.py _build_response updated to expose leave_travel and other_allowance. |
| 13.7 | Leave accrual engine | ✅ Done | `app/services/leave_engine.py`: 5 functions (check_and_end_probation, get_months_since_probation, calculate_monthly_cl_sl, run_monthly_accrual, encash_pl). `app/api/leave.py`: 5 endpoints (POST /run-monthly-accrual batch, POST /end-probation/{code}, GET /balance/{code}, POST /encash-pl/{code}, GET /accrual-log/{code}). Registered in main.py. Also fixed: LeaveBalance.balance marked Computed in model (was GENERATED ALWAYS in DB but missing Computed in ORM — would have caused INSERT errors). Fixed attendance.py PayrollMonth INSERT missing leave_travel/other_allowance from Session 13.6b. All 5 acceptance tests pass. | PL accrual uses `round(14/12, 3) = 1.167` per month; encashment deducts from entitlement (reduces DB-generated balance) and tracks in encashed_ytd. Idempotency tag = `MONTHLY_ACCRUAL:YYYY-MM` in leave_accrual_log.reason. Monthly CL/SL pattern driven by COUNT of prior CL accrual_log entries (not calendar date). |
| Block 8 | Cleanup + fix log | ⚠️ Partial | 8.2 golden test ✅, 8.3 no locked rows ✅. 8.1 (delete test employees) deferred by user. Test employees still in DB: UP000002–UP000006, US000001, US000002, UM000002. | DELETE these before Session 14 deploy or they will be in production data. |
| 13.8 | Attendance tab (frontend) | ✅ Done | `src/app/dashboard/attendance/page.tsx` built. Section 1: month/year picker + entity tabs (ALL/UPPL/USAPL/UAPL/UMPL). Section 2: paginated summary table (50/page, search, Export CSV, expandable rows → daily biometric breakdown). Section 3: 3-step import wizard (download pre-filled template → drag-drop upload → validate → commit). Section 4: payroll integration notice. Backend: added `GET /api/attendance/template` endpoint (StreamingResponse CSV with active employees' emp_codes pre-filled). `api.ts` extended with 5 functions: apiGetAttendanceSummary, apiGetAttendanceDaily, apiDownloadAttendanceTemplate, apiAttendanceImportValidate, apiAttendanceImportCommit. Sidebar already had /dashboard/attendance ✓. Zero TypeScript errors. Post-session fix: attendance CSV first column renamed from "HRMS Code" → "Emp Code" (uses database emp_code e.g. UM000001 directly). Parser reads "Emp Code" first, falls back to "HRMS Code" for backward compat with old files. | ALL entity tab fetches 4 entities in parallel for super_admin; non-super_admin ALL = their entity. Import wizard shows entity selector only for super_admin when ALL tab active. Template uses utf-8-sig encoding for Excel compatibility. |
| 13.9 | Payslip PDF redesign + Leave balance | ✅ Done | DB migration: `ALTER TABLE employees ADD COLUMN pf_number VARCHAR(30)`. Model updated (`pf_number = Column(String(30))`). Schemas updated (pf_number in EmployeeCreate/Update/Response). `_build_response()` in payslip.py redesigned: proration factor (pay_days/total_days, defaults 1.0 when no attendance), rate/amount pairs for all earning components, total_earnings (prorated), gross_rate (full monthly), net_pay = total_earnings - total_deduction (prorated), department via Department model, leave_balances dict from LeaveBalance table, pf_number/uan_no/esi_no from Employee model, month_year (e.g. "APRIL - 2026"). `payslip_template.html` completely redesigned: 6-column table (Attendance | Earning | Rate | Amount | Deduction | Amount), 2-column employee info block (Name+Dept | Desig+PF No+ESI No+Series+UAN+OT), conditional rows for LEAVE TRAV/DA/OTHER ALL/ESIC/PT/Loan, footer rows showing Gross Rate + Total Earnings + Total Deductions + Net Pay. Frontend payslips/page.tsx: PayslipData type extended with 20 new fields, PayslipDocument component rebuilt with 6-col table + 2-col emp info + live leave balance display, stats bar now shows Total Earnings (prorated) instead of full Gross. Golden test UM000001 Apr 2026: gross_rate=9349, net_pay=8493 ✓ | other_allowance excluded from proration (always paid in full) but included in total_earnings. Deductions (PF/ESIC/PT) are never prorated — always based on full monthly statutory gross. |
| 13.10 | Login redesign + Logo + UI animations (REVISED — liquid glass) | ✅ Done | **Superseded the original blob design with a faithful 1:1 port of the approved "liquid glass" Canva design.** Shared scene `src/components/auth/LiquidGlassScene.tsx` (dark radial backdrop, 3 drifting oklch orbs, 2 chevrons, fractal-noise grain, vignette, 8 rising sparks, frosted card with blur-up entrance + sweep sheen + cursor specular; `useEffect` drives pointer parallax, 3D tilt, eased cursor-trailing glow, magnetic ripple button, Enter-to-submit; all motion gated by `prefers-reduced-motion`). `login/page.tsx` + `change-password/page.tsx` render their form as `children` inside it. **Fonts:** Saira + IBM Plex Sans via `next/font/google` (`--font-saira`/`--font-plex`); rest of app keeps Inter. **Logo (final):** single transparent `/udyogi-logo.png` everywhere; white-flatten filter REMOVED from `Logo.tsx` (it killed brand colors); Sidebar wraps logo in a light `bg-white/95` chip for legibility on dark chrome. **App-wide motion** in `globals.css`: `--ease-expressive`/`--ease-smooth`, `.rise-in`+`.delay-1..5`, `.card-hover`, `.press`; Sidebar active = sliding `scaleX` red bar + staggered nav; Header tabs/logout `.press`; dashboard cards `.card-hover`+staggered `.rise-in`. `tsc`, eslint, `next build` all clean. | Full detail in the "Deviations" notes below (13.10 + 13.5 entries). `Udyogi-logo-liq.png` is unused/unusable (baked-in glass panel) — safe to delete. |
| 13.11 | Bug fix: employees table + CSS animations | ✅ Done | **Issue 1 — Employees not showing:** Silent `catch { setEmployees([]) }` was hiding all API errors. Fixed: added `fetchError` state, `console.error`, visible red error message in table empty-state, and a Retry button. Root cause of blank table was 471MB stale `.next` cache — deleted and dev server restarted cleanly. **Issue 2 — UI animations/redesign not visible:** Same stale cache issue — old compiled JS/CSS was being served. Fixed: cleared cache + restarted server. Also moved `.animate-blob`, `.animate-blob-delay`, `.animate-slide-up`, `.no-scrollbar` into `@layer utilities {}` in globals.css — in Tailwind v4, `animate-*` classes defined outside `@layer utilities` can be silently overridden by Tailwind's own animation pipeline. **Password note:** UP000001 password reset to `Admin@2026` during diagnosis (old password was unknown after user changed from first-login flow). | Dev server restart clears all hot-reload stale states. Always restart after large multi-file sessions to ensure changes propagate. |
| 13.13 | Punch-in/out bug fix + 5-min lock + deactivated | ✅ Done | **Bug fix:** `attendance_raw.punch_type` must be `'IN'`/`'OUT'` (not `'I'`/`'O'`); `attendance_raw.source` must be `'manual'` (not `'web'`); `attendance_daily.att_status` must be `'present'` (not `'P'`) — all three DB check constraints were violated causing silent 500. Fixed values in `attendance.py`. Added visible error feedback in `PunchCard`. **5-minute lock:** after punch-in, punch-out button is disabled and shows countdown ("Punch out in Xm Ys"); lock reads from server `first_in` timestamp so it survives page refresh; tick interval changed from 60s to 10s for countdown accuracy. **Deactivated:** `PunchCard` removed from dashboard render — backend endpoints (`GET /api/attendance/today`, `POST /api/attendance/punch`) and component code fully intact, ready to re-enable. | Re-enable by restoring the grid + `<PunchCard />` in `dashboard/page.tsx` around the LeaveApplyCard. |
| 13.12 | Leave apply on dashboard + PL cash always visible + punch-in/out | ✅ Done | **Leave tracker:** removed click-to-reveal; PL cash column now always shows value for every employee; summary stat card shows total PL cash across all filtered employees (not selected-only). **Dashboard:** `LeaveSection`/`GoalBar`/`CelebrationBanner` removed; leave apply card now visible to ALL roles (not just employees); `PunchCard` added beside leave apply (side-by-side grid on lg+, stacked on mobile). **Backend:** `GET /api/attendance/today` returns today's punch status; `POST /api/attendance/punch` auto-detects in/out from daily state, writes to `attendance_raw` (source=`web`) + upserts `attendance_daily`, calculates hours_worked + ot_hours on punch-out, audit-logged. **Frontend:** `TodayAttendance` + `PunchResult` interfaces and `apiGetTodayAttendance`/`apiPunch` added to `api.ts`. `PunchCard` shows In/Out times + live elapsed timer (ticks every 60s while clocked in); green Punch In → red Punch Out → locked "Attendance recorded" state after punch-out. | — |
| 14.5 | About tab | ✅ Done | **Consolidated `src/lib/appMeta.ts` into one `APP_META` object** (name, version 1.0.0, releaseDate 2026-06, developer "Sanndip Roy", publisher/copyrightHolder "Udyogi Group", description) — single source of truth; **Credits page (14.4) refactored** from the old individual constants to `APP_META`. New "About" sidebar item in the **Info** group (`/dashboard/about`, Info icon, `employee:true` → all roles) + added to `EMPLOYEE_ALLOWED_PREFIXES`. `dashboard/about/page.tsx` — identity card (reuses `<Logo>` + version badge + release date), description, "What it does" feature grid (Employees & multi-entity, Attendance & biometric, Payroll & statutory engine, Leave accrual & requests, Loans & advances, Locations/GSTN, Payslip PDF), factual About blurb, version/release + link to Credits. **+ "Technology stack & licenses"** 3-column table (Component · License · What it does), grouped Backend/Frontend/Infrastructure, sourced from the real `requirements.txt` + `package.json` (FastAPI/Uvicorn/SQLAlchemy/Alembic/Pydantic/psycopg2/python-jose/argon2/pandas/openpyxl/WeasyPrint/Jinja2; Next.js/React/TS/Tailwind/Zustand/Axios/jwt-decode/Recharts/lucide/ESLint; PostgreSQL/pgcrypto/Docker/Caddy). **Acceptance:** about renders all roles; **app_version single-source = 1.0.0** (frontend `APP_META.version` === backend `app/core/version.py` APP_VERSION, which system-stats returns); tsc clean. | Version bump point: edit `APP_META.version` (frontend) AND `app/core/version.py` (backend) together — two files, same value (frontend can't import backend). |
| 14.4 | Credits tab | ✅ Done | **Backend:** `app/core/version.py` (`APP_VERSION="1.0.0"`, single source) + `app/api/meta.py` → `GET /api/meta/system-stats` (super_admin+entity_admin, read-only, no audit): employees_total/active, entities, locations_active, payroll_months_processed/locked, loans_active, db_table_count (information_schema), app_version, server_time — **entity-scoped** for entity_admin (own entity; entities=1; locations include NULL group-wide). api.ts `apiGetSystemStats` + `SystemStats`. **Frontend:** `src/lib/appMeta.ts` (APP_NAME/APP_VERSION/DEVELOPER/PUBLISHER/COPYRIGHT_HOLDER — **single source, placeholders for Abhishek to fill**, reused by 14.5 About); new "Info" sidebar group → **Credits** (`/dashboard/credits`, Award icon, `employee:true` so ALL roles see it) + added to `EMPLOYEE_ALLOWED_PREFIXES`; `dashboard/credits/page.tsx` — Designed/Published/Copyright (© {clientYear} {holder}) cards + System statistics grid (**admin-only**; employees see "Statistics available to administrators", page never calls the admin endpoint for them so no 403/crash). **C1–C3 pass:** stats live (super 11/4/24/36/5loans/27tbl, entity_admin scoped 8/1/4), employee→403 on endpoint but page hides block gracefully; renders for all roles; tsc clean. | Credits text in `src/lib/appMeta.ts`: **DEVELOPER = "Sanndip Roy"** (set by Abhishek); PUBLISHER + COPYRIGHT_HOLDER = "Udyogi Group" (defaults — change if needed). Keep `appMeta.APP_VERSION` in sync with backend `app/core/version.py`. |
| 14.3b | Dummy loan data (DELETE before deploy) | ⚠️ Demo data | Inserted **6 dummy loans** via the API to populate the Loans UI: id2 UM000002 loan 60000/5000 (Jan+Feb 2026 processed → outstanding 50000), id3 US000001 advance 12000/2000 **paused**, id4 UP000002 loan 100000/10000, id5 UP000004 advance 8000/4000, id6 UP000005 loan 36000/3000, id7 UP000003 other 15000/2500. Golden UM000001 left loan-free (net 8493). **CLEANUP before deploy:** `DELETE FROM loans;` (cascades `loan_emi_schedule`). Also note this created `payroll_months` rows for UM000002 Jan/Feb 2026 (UM000002 is itself a disposable test row already slated for deletion). | Demo only — not real loans. |
| 14.3 | Loan / advance module | ✅ Done | Loans + per-month editable EMI ledger that auto-deducts on payroll. **Schema (`008_loans.sql`):** `loans` (principal/emi/tenure/start/end/outstanding/status active·paused·closed·written_off, updated_at trigger) + `loan_emi_schedule` (scheduled_emi, **actual_emi** override target, is_overridden, applied, UNIQUE(loan_id,year,month)). Models + exports. **Service (`loan_service.py`):** invariant **outstanding = principal − Σ(actual_emi where applied)**; `apply_emi_on_payroll` (idempotent — `applied` boolean, never double-decrements), `set_month_override` (0=skip / reduced, blocks locked months), `recompute_outstanding`, `get_emi_for_period`, `ensure_schedule_row` (caps last EMI to remaining). **Engine:** `process_payroll_month` sets `loan_emi = apply_emi_on_payroll(...)` then recomputes `total_deduction`+net in Python. **API (`app/api/loans.py`):** GET list (entity-scoped) / GET {id}+schedule / GET employee/{emp} (own or admin) / POST / PUT (principal locked after any applied EMI→400) / POST override (reason≥4, 400 on locked) / POST close — audited. **Frontend:** Loans sidebar tab (HandCoins, adminOnly); `dashboard/loans/page.tsx` (table EMP ID·NAME·TYPE·PRINCIPAL·EMI·OUTSTANDING·TENURE·START·END·STATUS·ACTIONS, entity tabs+search+status filter, summary cards, Add modal w/ employee picker + end/total preview + mismatch warning, Manage-months drawer w/ inline override, Edit, Close); api.ts CRUD. **All F1–F9 pass** (Apr deduct 1000→outstanding 11000; May override 0→no deduct; Jun override 500→10500; locked-override 400; principal-after-applied 400; **golden 9349/785/71/0/8493 loan_emi=0**; unlock+reprocess no double-count; employee own 200/other 403; tsc clean; schema re-dumped). Test loan on UM000002 cleaned up (0 loans remain). | net_pay: `loan_emi` feeds the DB-GENERATED `total_deduction` (pf+esic+pt+loan_emi+other_deduction) — engine recomputes the same formula in Python after setting loan_emi and never passes total_deduction to the constructor. **Unlock/reprocess reconciliation:** `applied` is a boolean (not additive); reprocess returns the stored `actual_emi` without re-decrementing, and `recompute_outstanding` rebuilds balance from the ledger → no drift. Filename `008_` (006/007 taken). Attendance-import path creates rows with loan_emi=0; they self-heal to the correct EMI on the next payslip view/reprocess (process_payroll_month is the loan-aware path). |
| 14.2 | Prominent figures (UP000001–UP000008) | ✅ Done | Seeded the 8 company figures at exact emp_codes, names locked. **Reconcile (`006_seed_prominent_figures.sql`, idempotent):** UP000001–004 existed as **test rows → renamed** (Priya Sharma→MANOHAR BAGRI, Anil Mehta→SUDHIR KUMAR MUNDHRA, Sunita Rao→NATWAR BAGRI, Pooja Patel→MUKUND GOPALDAS BAGRI); UP000005–008 were **MISSING → inserted** (SHEETAL MUNDHRA, PRITAM DEB, SANDIP ROY, NISHA BAGRI). Each new row: entity UPPL, location KOLKATA, status active, is_on_probation false, salary 0, **placeholder doj=2000-01-01 + mobile=0000000000 (HR must correct)**, + an `initial` all-0 salary_structure (keeps the 13.15 one-active-structure invariant). Audited `SEED_PROMINENT_FIGURE` (placeholder flag in audit since employees has no remarks col). **UP000001 super_admin auth/role/password untouched** (only its name changed). `generate_emp_code('UPPL')` is MAX+1 → next = **UP000009** (verified, no collision). **Protection (`007_protect_figures.sql` + employees.py):** `PROTECTED_FIGURE_CODES`={UP000001..UP000008}; PUT blocks name change (400, other fields editable), DELETE blocks (400); DB trigger `trg_protect_company_figures` raises on name-change/delete as belt-and-braces. **All F1–F7 pass** (8 present/ordered, next UP000009, PUT name 400 / mobile 200, DELETE 400, UP000001 login OK super_admin, golden 9349/8493, tsc clean, schema re-dumped). | ⚠️ **Test-row repurposing:** UP000002/UP000003/UP000004 were previously on the "delete before deploy" list — they are now **permanent figures, do NOT delete**. UP000003 keeps its `entity_admin` login, UP000004 its `employee` login (Test@1234) — auth untouched per spec; reset/disable those test logins before go-live if undesired. Remaining disposable test rows: **US000001, UM000002** only. UP000005–008 carry placeholder DOJ/mobile for HR to fix. |
| 14.1 | Locations (GSTN) module | ✅ Done | Replaced the 9 legacy city-codes with the **24 client GSTN unit-locations**, remapped all employees, added an editable **Locations (GSTN)** admin tab. **Migration** (`backup_locations_employees_pre_14_1.sql` + `005_locations_gstn.sql`, one txn): dropped v_employee_full/v_payslip_summary → widened `locations.id` + 3 FK `location_id` cols to varchar(40) → added `locations.status`(active/inactive CHECK)+`phone` → inserted 24 (id=slug of name, city='', entity_id=NULL, WB GSTN repeats per state) ON CONFLICT → remapped employees+biometric_mapping(attendance empty) via report temp table → abort-if-any-legacy-ref guard → deleted legacy 9 → audit LOCATION_REMAP → recreated views. **Backend:** Location model (+status,+phone, id/location_id→String(40)); new `app/api/locations.py` (`GET /api/locations` entity-scoped, `GET /active`, `POST` slug+dup-400+pt-validation, `PUT` id-immutable, `DELETE` 409-if-referenced else soft-deactivate / `?hard=true`), all super_admin+entity_admin, audited. **Frontend:** sidebar "Locations (GSTN)" (MapPin, adminOnly); `dashboard/locations/page.tsx` (table + search/state/status filters + add/edit modal w/ live slug preview + delete→409→deactivate); api.ts CRUD; **employee add+edit location dropdowns switched to `/locations/active`** (the 24 are entity-NULL so the old entity filter hid them). **All F1–F7 pass.** | ⚠️ **GOLDEN INTEGRITY:** UM000001 was on `jpr` (PT=NIL). The default `jpr→JPUR-SHARED` is **West Bengal (PT=WB)** — would break the golden test. The 'jpr' letters collide: golden Jaipur=Rajasthan/NIL vs client JPUR units=WB. **Resolved: UM000001 special-cased `jpr→NOIDA` (PT=NIL preserved)** → golden still 9349/785/71/0/8493 ✅. Placeholders flagged in remap report for HR: US000001 `dadra→CHENNAI` (NIL preserved), test-row UM000002 `jpr→JPUR-SHARED` (PT NIL→WB, disposable). Filename is `005_` (004 taken by salary cols). Views are legacy (app uses ORM) — dropped/recreated verbatim. |
| 13.21 | Remove People + Reports sidebar tabs | ✅ Done | Removed the "People" (`/dashboard/people`) and "Reports" (`/dashboard/reports`) nav items from `src/components/layout/Sidebar.tsx` — they were the only two items in the "Management" group, so the whole group was dropped, plus the now-unused `UserCog` import (`BarChart3` kept — still used by the Payroll item). No other references anywhere (BottomNav / More / quick actions clean). `tsc` exit 0. Also re-dumped `backend/sql/schema_snapshot.sql` (confirmed current — frontend-only change, no DB diff; 23 tables / 2 views). | — |
| 13.20 | Add missing salary columns (schema only) | ✅ Done | `backend/sql/004_add_missing_salary_columns.sql` — added the client-Excel salary fields that were missing. **Earnings** `conveyance`, `medical`, `other_earning` → `employees` + `salary_structures` + `payroll_months`. **Per-month** `salary_arrears` (one-off earning) + deductions `income_tax` (ITAX/TDS), `lwf` (labour welfare), `nps` → `payroll_months`. All `numeric(10,2) NOT NULL DEFAULT 0`. **Schema only** — ORM models, payroll engine, statutory treatment, and frontend are NOT wired yet (deferred until treatment decisions land). `payroll_months.total_deduction` GENERATED expr unchanged, so income_tax/lwf/nps are stored but not yet summed. **Held** (semantics pending client): `payroll_series` (values General/ATT) + `salary_att`. Golden test still 9349/8493 (new cols default 0); `schema_snapshot.sql` re-dumped. | Adding NOT NULL DEFAULT 0 cols is safe for existing app inserts (ORM omits them → DB default applies). Wiring them into gross/PF/ESIC/PT/net needs the per-component statutory matrix first (see scope discussion). |
| 13.19 | UI polish from `impeccable` critique (P1 contrast/legibility + P2/P3 deferred) | ✅ Done | Ran the `impeccable` design-critique skill (score 29/40, product register); applied the findings. **P1 contrast:** replaced the failing muted gray `#9A9A9A` (~2.85:1) with pure-gray `#6B6B6B` (~5.3:1) across all 14 dashboard pages + sidebar/layout; fixed the global `input/textarea::placeholder` rule (was `rgba(21,23,27,0.42)`). **P1 glass legibility (kept the client-approved glass identity):** raised the white content surface alpha `0.84 → 0.96` in `GlassCard.tsx` + `.glass-card` (globals.css) so dense tables read cleanly while keeping the frosted blur. **P3 entity colors:** new `lib/entities.ts` (`ENTITY_COLORS`/`entityColor`) as single source of truth; **USAPL given a distinct teal `#0D9488`** (was ink `#1A1A1A`/gray, indistinguishable); refactored leave tracker, employee detail, store `ENTITIES`, dashboard `ENTITY_META` to use it. **P2 de-dup:** extracted `components/salary/SalaryHistoryTable.tsx` (shared by Payroll console + employee detail) and `components/salary/IncrementModal.tsx`; removed the inline copies. **P2 skeletons:** new `components/ui/Skeleton.tsx` (`Skeleton`/`SkeletonRows`); replaced content spinners with skeleton rows in the payroll status table, salary history, and payslip loading. **P2 detector hit:** Payroll entity tabs no longer use `border-b-2` on `rounded-full` (the underline-on-pill clash) — selected state is now a filled pill in the entity's color. `tsc` clean; impeccable detector returns `[]` (was 1 warning). Snapshot not persisted (no `.impeccable/` files created). | Glass is a committed brand decision (13.10), so the fix was legibility-within-glass, not removing it. Still open (not requested this pass): power-user efficiency (bulk "close month", keyboard nav, status legend). |
| 13.18 | Bugfix: recharts width(-1)/height(-1) chart warning | ✅ Done | Dashboard "Entity headcount" bar chart (the only `ResponsiveContainer` in the app) mounted on the first render, so recharts measured its parent before layout settled → `width(-1)/height(-1)` console warning (doubled by React StrictMode in dev). Fix in `src/app/dashboard/page.tsx`: added a `chartReady` flag set in a mount `useEffect` and gated the `ResponsiveContainer` behind it, so the chart renders one tick later against a laid-out `h-44` parent (wrapper still reserves the space → no layout shift). Also added `minHeight={0}` alongside the existing `minWidth={0}`. `tsc` clean. | The earlier 13.5 `minWidth={0}` only partially suppressed it; deferring the mount is the reliable fix. Clear `.next` cache + hard refresh if the warning lingers. |
| 13.17 | Payroll console (Tier 2) + lock/unlock + status APIs | ✅ Done | **Prompt was headed "13.16" but that number was already used (leave tracker) → logged as 13.17.** Built the Payroll tab into an operations console. **Backend (Part A):** added a second `payroll_router` in `app/api/payslip.py` (mounted at `/api/payroll` in main.py — keeps code with process-month but gives correct paths). `GET /api/payroll/months` (aggregated one row per entity/year/month: employee_count, derived status [all locked→locked / all draft→draft / else processed], total_net/gross, locked/processed/draft counts, max locked_at; entity-scoped, super_admin all), `POST /api/payroll/lock` (super_admin+entity_admin, sets status='locked'+locked_at=now(), idempotent, 400 if no rows, audit PAYROLL_LOCK), `POST /api/payroll/unlock` (super_admin ONLY, reason ≥5 chars required→400, sets status='processed'+locked_at=NULL, 400 if not locked, audit PAYROLL_UNLOCK with reason — only place actor+reason are recorded). **Frontend (Parts B/C):** api.ts +apiGetPayrollMonths/apiProcessMonth/apiLockPayroll/apiUnlockPayroll + PayrollMonthRow/ProcessMonthResult types. New `src/app/dashboard/payroll/page.tsx` — segmented control [Monthly Run | Salary Structures]; Monthly Run = entity tabs (super_admin) + year filter + status table (Process/Lock/Unlock gated by status+role, confirm modals, unlock requires reason, process-error list, "Run a new month" row, empty state); Salary Structures = debounced employee picker → read-only salary-history table + Apply Increment modal (26th validation, prefilled active components, live new-gross). `useSearchParams` wrapped in Suspense; reads `?emp=` to auto-select. **Part D:** employee detail page salary-history is now read-only — Apply Increment button+modal removed, replaced with a link "Apply an increment from the Payroll tab →" to `/dashboard/payroll?emp={code}`. **Part E:** sidebar Payroll nav already correct (`/dashboard/payroll`, adminOnly). **All F1–F11 pass** (lock/unlock RBAC + audit + reason validation; lock guard intact; golden 9349/8493; tsc clean). Post-test DB clean: 0 locked rows. | Roles: prompt said `hr` throughout but `hr` was removed in 13.14j — collapsed to super_admin/entity_admin (lock) and super_admin-only (unlock); the "hr→403" tests were validated with a plain employee + cross-entity admin instead. `payroll_months` has no `entity_id` → all aggregation/scoping joins `employees`. UM000002 (test employee, slated for deletion) gained May/June 2026 rows during process tests — fine, will be removed pre-deploy. |
| 13.16 | Leave tracker → balances only | ✅ Done | Stripped the Leave tracker (`src/app/dashboard/leave/page.tsx`) down to leave balances per user request. Removed: "Encash eligible" + "Year streak" summary cards (only "Active employees" remains), the **PL Cash value** and **Streak** table columns + the green "Eligible" PL badge, the mobile card's cash-value/streak row, and the detail panel's stats row (PL Cash Value / Encash Eligible / Year Streak) — detail panel now shows only CL/SL/PL chips. Subtitle → "Leave balances for all active employees". Cleaned up unused imports (`Flame`, `TrendingUp`, `Wallet`), the `fmt` helper, and `PL_ENCASH_THRESHOLD`. Backend `apiLeaveTracker` response untouched (display-only change). `tsc` clean. **Follow-up tweak:** centered the CL/SL/PL chips — added `justify-center` to the detail-panel balances row and the mobile card pill row (layout-only). | — |
| 13.15 | Salary structure history + effective-dated increments | ✅ Done | **salary_structures is now the source of truth; employees salary columns are a synced cache of the active structure.** Part A: `sql/003_salary_structures.sql` — created table (effective_from/effective_to, 7 components, reason CHECK initial/increment/correction), 3 indexes incl. partial UNIQUE `uq_salstruct_active` (one NULL effective_to per emp); backfilled 'initial' active structure for all 7 employees from `doj` (spec said date_of_joining — adapted to actual column `doj`; COALESCE all components NOT NULL). Part B: `SalaryStructure` model + export in `models/__init__.py`. Part C: `services/salary_resolver.py` (`get_structure_for_period`, `get_active_structure`). Part D: `services/increment_service.py` `apply_increment()` (26th-boundary guard, backdate guard, carry-forward of omitted components, syncs employee columns, recomputes esic_applicable, audit action=INCREMENT) + endpoints `POST /api/employees/{code}/increment` & `GET /api/employees/{code}/salary-history` (employee can view own; admins entity-scoped). Part E: `compute_payroll()` now resolves the period's structure as the salary SOURCE (signature unchanged — resolution lives inside it since it already has emp_code/year/month; falls back to live employee columns when None). Part F: payslip `_build_response` adds `salary_effective_from` (+ `_display` DD-MMM-YYYY); template + frontend payslips page render "Salary w.e.f". Part G: employee detail page — Salary history table (active row green pill, historical muted) + Apply Increment modal (26th validation, prefilled components, live new-gross, reason); `apiGetSalaryHistory`/`apiApplyIncrement` in api.ts. **All H1–H7 pass** (golden UM000001 Apr 2026 → 9349/8493 unchanged; increment carry-forward; period resolution old-vs-new; boundary+backdate 400s; tsc clean). | Increments ALWAYS align to the 26th payroll-cycle boundary (no mid-period proration — one payslip = one structure). Roles: spec said `hr` but hr was removed in 13.14j — endpoints use `super_admin`+`entity_admin`. `apply_increment` flushes the close (effective_to) BEFORE inserting the new active row so the non-deferrable partial unique index never sees two active rows. UP000001 was used for H3/H4 per spec then **restored to baseline** (single 'initial' structure, basic 30000); UM000002 used for the flush re-test then restored. |
| 13.14n | Leave status tracker on dashboard | ✅ Done | Added `MyLeaveRequestsCard` to dashboard (below Apply leave card) so applicants can see their own request statuses. Shows each request: leave-type badge (CL/SL/PL colored), date range + days + reason, and a status pill — Pending (amber) / Approved (green) / Rejected (red) / Cancelled (gray). Empty state "No leave requests yet." Data from existing `GET /api/leave/my-requests` (scoped to logged-in user, any role). **Auto-refresh:** `LeaveApplyCard` now fires `onApplied` → bumps a `leaveRefresh` key so the list updates immediately after applying; also polls every 30s so approve/reject shows without manual reload. Visible to all roles (admins see their own too). `tsc` clean; verified against UM000001 real data (approved + rejected rows). | Both leave flows (`/apply` legacy + `/request`) write to the same `leave_requests` table, so `/my-requests` shows requests from either. |
| 13.14m | Remove duplicate leave-apply on dashboard | ✅ Done | Dashboard showed leave-apply twice: the inline `LeaveApplyCard` form AND an "Apply leave" navigation tile in Quick actions (both added at different times — 13.12 inline card + quick-action tile). Kept the **inline form card** (apply without leaving the page); removed the redundant tile from `EMPLOYEE_ACTIONS` and the "My leaves" tile from `QUICK_ACTIONS`. Removed now-unused `CheckSquare` import. Standalone `/dashboard/leave/apply` page still reachable via sidebar "Apply leave". `tsc` clean. | The two "Apply leave" `<h2>` headers inside LeaveApplyCard are worker/probation vs normal branches — only one renders, not a duplicate. |
| 13.14l | Remove "Latest payslip" dashboard card | ✅ Done | Removed the Latest payslip KPI card from `dashboard/page.tsx` entirely + cleaned up everything it used: `latestPayslip` state, the 6-month-walkback `useEffect` that fetched it, and the now-unused `apiGetPayslip` import. Resulting KPI grid — employee: Status card only; admin: Status + Total employees + Pending approvals. Payslips still reachable via sidebar "My payslips" + quick-action. `tsc` clean. | — |
| 13.14k | Full-system verification (frontend↔backend) | ✅ Done | Ran ~31 API test cases across auth, RBAC (all 3 roles), payroll, leave, locked-payroll. **All functional pass.** Auth: bad-pw/no-token/bad-token→401, valid→200. RBAC employee: own payslip 200, other 403, master-data 403, create 403, statutory 403, holidays GET 200. RBAC entity_admin: master-data 200, own-entity 200, **cross-entity payslip 403** (scoping verified), statutory PUT 403. super_admin: full access. Golden UM000001 Apr→9349/785/71/0/8493 ✅. ESIC auto (30000→0) ✅. PT MH 200/GJ 150/NIL 0 ✅. emp_code immutable→400 ✅. Salary-edit→payslip reflects immediately ✅. Leave: self-apply 201/other 403/approve 200/pending-count 200 ✅. Locked payroll stays locked on view + batch puts it in errors[] (no crash) ✅. Frontend `tsc` clean. **Test accounts (all Test@1234): UP000003=entity_admin(UPPL), UP000004=employee(UPPL), UM000002=employee(UMPL); UP000001=super_admin & UM000001=golden both Admin@2026.** | eslint has 19 PRE-EXISTING advisory errors (mostly `react-hooks/set-state-in-effect` across ~12 untouched files) — not regressions, but would block a strict `next build`; clean up before deploy. |
| 13.14j-pre | Employee RBAC restriction (frontend) | ✅ Done | Plain employees locked to Dashboard + own payslips + leave apply only. Added `isEmployee()`, `EMPLOYEE_ALLOWED_PREFIXES`, `isEmployeeAllowedPath()` to `lib/auth.ts`. **Centralized route guard in `dashboard/layout.tsx`** — employee hitting any non-allowed route (incl. typed URL) redirects to `/dashboard` (covers all current+future pages). Sidebar + mobile BottomNav show only employee-allowed nav. Dashboard: hid headcount chart, total-employees/pending-approvals cards, admin quick-actions for employees. Payslips page already employee-safe (picker is admin-only; backend `_assert_access` enforces own-only). | Defense-in-depth: frontend UX layer; backend RBAC (require_role, entity scoping, _assert_access) is the real gate. |
| 13.14j | Simplify to 3 roles (employee/entity_admin/super_admin) | ✅ Done | Removed `hr` and `manager` roles system-wide (no such users existed: 6 employee + 1 super_admin). **Backend:** every `require_role(...,"hr"[,"manager"])` collapsed to `("super_admin","entity_admin")` across admin/employees/leave/attendance/biometric.py; removed manager reporting-chain branch in attendance daily view; **DB constraint tightened** → `users.role CHECK IN ('super_admin','entity_admin','employee')`. **Frontend:** `UserRole` type → 3 roles; `isAdminRole` → super_admin+entity_admin; removed all `=== "manager"` checks (layout/dashboard/approvals/BottomNav); removed `"hr"` from leave-tracker ALLOWED_ROLES + attendance canImport. Verified: super_admin admin endpoints 200, employee blocked 403, `tsc` clean, backend syntax OK. | Admin tier is now entity_admin (entity-scoped) + super_admin (all entities). |
| 13.14i | Bugfix: payslip showed stale salary + dummy test data | ✅ Done | **Bug:** payslip data + PDF endpoints only reprocessed a `payroll_months` row when missing or `status=="draft"`, so once `"processed"` the snapshot was frozen — editing an employee's salary did NOT update the payslip (showed old numbers). **Fix:** `app/api/payslip.py` both endpoints now reprocess when `status != "locked"` (recompute unlocked rows from current salary/attendance on view/download; locked rows stay frozen per rules 5 & 6). **Golden fixture:** discovered UM000001's salary had been changed to gross 17600 during testing → restored to golden (basic 5542/hra 2000/da 1000/spl 807 = gross 9349); golden test re-verified PASS (9349/785/71/0/8493). **Test data added:** 5 dummy employees via API for salary-edit testing — UP000002 Anil Mehta (UPPL/Pune, gross 20000, ESIC+MH PT), UP000003 Sunita Rao (UPPL/Kol, gross 30000, ESIC N/A+WB PT), US000001 Vikram Singh (USAPL/Dadra, gross 13000, NIL), UP000004 Pooja Patel (UPPL/Vapi, gross 11000, worker+GJ PT), UM000002 Rahul Verma (UMPL/Jpr, gross 17000, NIL). Covers ESIC on/off boundary, all 4 PT states, staff/worker. | **DELETE these 5 test employees before VPS deploy.** Use them (not UM000001) for salary-edit tests. |
| 13.14h | Mobile login dead — Next dev LAN-origin block | ✅ Done | **Root cause:** Next.js 16 blocks its internal dev assets (`/_next/*` JS chunks + HMR) from origins not in `allowedDevOrigins`. Accessing the dev server over the Mac's LAN IP (`192.168.7.171:3000`) from a phone = disallowed origin → chunks blocked → **React never hydrated on any phone** → every button/link dead, page auto-reloading. `localhost` is always allowed → laptop always worked. **Fix:** `next.config.ts` → `allowedDevOrigins: ["192.168.7.171"]`, then full dev-server restart (config changes need restart). Confirmed working on iPhone 17/iOS 26 Safari + Android. **Diagnostic proof:** a bare `<button onClick={alert}>` outside the glass card was also dead → isolated it to "JS not running," not button code. On-screen error catcher showed `Script error.` (cross-origin/blocked chunks). **Red herrings (reverted):** earlier guesses — `preserve-3d`→`flat`, backdrop-filter behind-layer, magnetic-button touch-gating — were NOT the cause; nothing was wrong with the button code. Kept the touch-safety changes anyway (harmless + correct); restored magnetic/ripple effects gated to desktop. | Mac IP is hardcoded in `next.config.ts` + `.env.local` — update both if DHCP changes it (or widen to subnet wildcard). |
| 13.14g | Leave tracker: click-to-expand detail panel | ✅ Done | Removed "Total PL cash"/"PL liability" stat card (page-load ₹ aggregate was confusing) → top grid now 3 cards (Active employees, Encash eligible, Year streak), `grid-cols-1 sm:grid-cols-3`. Added `selectedEmpCode` state + derived `selectedEmployee`; desktop table rows + mobile cards now clickable (toggle: re-click deselects), selected row tinted `bg-[#E5202E]/[0.06]`. New detail panel below table (GlassCard + `.rise-in` fade/slide): header (name · code · entity pill · X close), 3 leave-balance chips (CL/SL used/entitlement, PL balance), 3 stat items (PL Cash Value/Wallet, Encash Eligible/TrendingUp, Year Streak/Flame). `useEffect` resets selection when employee leaves filtered view. Animation uses existing `.rise-in` (tailwindcss-animate not installed). Encash Eligible kept at `PL_ENCASH_THRESHOLD = 28` (codebase-wide convention, matches leave_policies.min_balance_encashment) — spec's acceptance-test example claiming PL=21 → "Yes" was an inconsistency, deliberately not followed. `tsc --noEmit` clean. | Bugfix-mobile-buttons (13.14*) preceded this. |
| 13.14f | Bugfix: login page auto-refresh loop | ✅ Done | Stale `auth_session` cookie + expired localStorage tokens caused a cycle: proxy bounced `/login` → `/dashboard` → API calls 401 → refresh failed → `window.location.href="/login"` (hard reload) → proxy saw cookie again → repeat. Looked like login page refreshing every few seconds. Fix 1: `auth.ts` `useAuth` now clears `auth_session` cookie before redirecting to `/login` when no token found — breaks the proxy bounce. Fix 2: `api.ts` interceptor skips hard redirect if already on `/login` — prevents reload mid-typing. Also need to manually clear stale `auth_session` cookie in browser DevTools once. | —  |
| 13.14e | Bugfix: payslip zeros + dashboard net pay card | ✅ Done | (1) `payroll_engine.py`: `pay_days=None` when no attendance caused `NOT NULL` violation on UPDATE — fixed to default `pay_days = total_days` when no attendance (consistent with proration=1.0). (2) Reprocessed all 12 payroll months for UP000001 (Priya Sharma) — now gross=45000, net=43000. (3) Dashboard "Latest payslip" card was hardcoded to "May 2026 / Net: ₹—" — replaced with live `apiGetPayslip` call that walks back up to 6 months to find the most recent processed row, shows month name + net pay in red, links to /dashboard/payslips. | —  |
| 13.14d | Bugfix: password reset + animation crash | ✅ Done | (1) UP000001 and UM000001 passwords were silently changed to `Admin@1234` during H-tests — reset back to `Admin@2026` to match documented state. (2) `LiquidGlassScene.tsx` line 78: `a.finish()` threw `InvalidStateError` on already-finished/idle animations — guarded with `a.playState === "running" \|\| "paused"` check. | —  |
| 13.14c | Real-time approval polling | ✅ Done | Added 30s `setInterval` polling in 3 files: `layout.tsx` (sidebar badge: was 60s → 30s), `approvals/page.tsx` `ApprovalQueue` (HR/admin queue auto-refreshes pending + all lists), `approvals/page.tsx` `MyRequestsView` (employees see status changes live), `dashboard/page.tsx` KPI card. All intervals cleaned up on unmount (`clearInterval`). | —  |
| 13.14b | Bugfix: leave approve 500 → CORS | ✅ Done | `PUT /api/leave/approve/{id}` was crashing with PostgreSQL `GeneratedAlways` error — old endpoint wrote `lb.balance = ...` directly but `leave_balances.balance` is `GENERATED ALWAYS AS (entitlement - used)`. Removed that line; DB computes balance automatically from updated `used`. Browser showed it as a CORS error (FastAPI 500s don't attach CORS headers). | —  |
| 13.14 | Public holidays + period calc + leave request engine | ✅ Done | **Part A** DB migrations: period_start/period_end/total_working_days on payroll_months; public_holidays table (56 holidays seeded: 3 universal + 53 location-specific); actioned_by/actioned_on/reject_note/applied_on/entity_id on leave_requests. **Part B** SQLAlchemy models: PublicHoliday, updated LeaveRequest, period cols on PayrollMonth. **Part C** `services/period_calculator.py`: period = 26th prev → 25th current; working_days = total − Sundays − mandatory holidays. **Part D** Attendance import: sets period fields on payroll_months + deducts CL/SL/EL from leave_balances on commit. **Part E** Payroll engine proration: pay_days/total_working_days (fallback /26, else 1.0); period fields set in process_payroll_month. **Part F** `api/leave.py` new endpoints: POST /request, GET /requests (paginated), PUT /request/{id}/approve, PUT /request/{id}/reject, GET /requests/pending-count. **Part G** `api/admin.py` holiday endpoints: GET /holidays?year=, POST /holidays, DELETE /holidays/{id}. **Part H** Acceptance tests H1–H7: all pass — period dates correct (2026-03-26→2026-04-25, 27 working days for jpr), 56 holidays returned, create/duplicate-block/delete holiday, leave apply+approve+reject flow end-to-end. | UM000001 was on probation — cleared for test; also seeded leave_balances for 2026. super_admin password was unknown — reset to Admin@1234 during test. |
| 14 | Docker + VPS deploy | ⬜ Pending — sign-off 6/8 | CLI checks all pass (health, login+audit, golden payroll, PDF 221KB, no 500s, audit log). Browser checks needed: (a) login page shows light glass card with real logo + dark background + blobs, (b) sidebar shows logo + red left-border active indicator, (c) payslip page renders with new layout for UM000001 Apr 2026 showing gross_rate=9349, net=8493. Complete these before running deploy.sh. | — |
| 13.5 | Local integration test pass (run after 13.11) | ✅ Done | Worked Blocks 0–8 end-to-end against native PG18 + live backend/frontend. **All backend blocks green; golden payroll test PASSES** (UM000001 Apr 2026 → gross 9349, pf 785, esic 71, pt 0, net 8493). Verified: auth (login/refresh/401-403/audit), employee CRUD (create UP000007 gross 16000, emp_code immutability 400, ESIC auto-compute), payroll (process-month batch, lock guard refuses locked rows untouched, PDF 1.9MB), admin (master-data export, reset-password `Udyogi@XXXX`, audit-log, statutory WB/MH/GJ/NIL, column-update diff w/ blank-skip), biometric (heartbeat OK, mapping upsert, punch→attendance_raw resolves to emp_code), known-deviation checks 7.1–7.6. Cleaned up all test employees, no locked rows remain, golden test re-confirmed (no regression). **1 bug fixed** (see deviations). Block 6 visual/interaction UI not auto-testable here — needs manual browser pass. | DB is native PG18 (`/Library/PostgreSQL/18/bin/psql -U postgres -d udyogi_hrms`, pw 2399), NOT Docker — prompt's `docker exec udyogi_db` commands were adapted. Prompt also used several outdated names (see deviations). |

---

## Key Facts Claude Code Must Know
- super_admin emp_code: UP000001 (already in employees table from seed)
- super_admin default password set by: `python scripts/create_superadmin.py`
- Payslip test: UM000001, Apr 2026 → Gross=9349, PF=785, ESIC=71, PT=0, Net=8493
- Legacy attendance CSV uses slash codes (UPPL/2026/00001) → map via legacy_code column
- eSSL biometric endpoint: POST /iclock/cdata (plain text ADMS protocol)
- VPS: raw IP, no domain, HTTP only (Caddy on port 80)
- Old Node.js system on same VPS — do NOT touch until Session 14
- **CRITICAL — PayrollMonth INSERT:** `total_deduction` is a `GENERATED ALWAYS AS` column in PostgreSQL. Never pass it to the `PayrollMonth(...)` constructor — PostgreSQL computes it automatically from `pf_emp + esic_emp + pt + loan_emi + other_deduction`. Passing it causes a hard 500 error. SQLAlchemy model has `Computed(...)` on this column. `net_pay` is NOT generated — always set it explicitly.

## Deviations from original plan
- Session 2: DB has 18 tables; `helpdesk_tickets` exists but was not in the model spec — omitted intentionally. 17 models built. SQL files were never written to disk; schema was introspected live from PostgreSQL.
- Session 3: `AuditLog.ts` and any `created_at` column that uses a DB-side `DEFAULT now()` must declare `server_default=sa_text("now()")` in the ORM model — otherwise SQLAlchemy sends explicit `NULL`, overriding the DB default. Fixed on `ts` only; remaining `created_at` columns should be patched similarly when they're first exercised.

*Add any changes here as the project evolves*
- Session 13.9: `_build_response()` net_pay is now COMPUTED (prorated) not the stored `pm.net_pay`. Formula: `net_pay = total_earnings - total_deduction` where `total_earnings = sum(prorated components) + other_allowance`. When no attendance (pay_days=None), factor=1.0 so prorated = full.
- Session 13.10: Login page is now a LIGHT glass card (rgba 0.82 white) on dark #121317 bg — changed from previous dark glass. `change-password/page.tsx` matches. CSS class `.animate-blob` (8s pulse) added to globals.css.
- Session 13.10: `Logo.tsx` default theme changed from "dark" to "light". Callers on dark backgrounds must pass `theme="dark"` explicitly. The brightness(0) invert(1) filter turns the entire logo white — looks correct only with a transparent-bg PNG. If logo has white bg, a transparent version (udyogi-logo-transparent.png) is needed.
- Session 13.10: Sidebar active nav item style changed from red-fill (`bg-[#E5202E]`) to dark glass + left red border (`bg-white/10 border-l-[3px] border-[#E5202E]`). This is more modern; easier to revert if needed.
- Session 13.9: `payslip_template.html` redesigned — 6-column layout with Rate column for earnings. Conditionally shows LEAVE TRAV, DA, OTHER ALL, ESIC, PT, Loan rows. Employee info is a 2-column table showing PF No, ESI No, UAN No, Dept, Series.
- Session 13.6b: `other_allowance` is excluded from statutory gross (PF/ESIC/PT base) but added to net_pay after all deductions: `net_pay = gross - total_deduction + other_allowance`. It is snapshotted in payroll_months.
- Session 13.6b: `leave_travel` IS part of statutory gross: `gross = basic + hra + da + spl + cca + leave_travel`. ESIC eligibility check uses this statutory gross (other_allowance never affects ESIC).
- Session 13.6b: `leave_policies` table has UNIQUE(entity_id, category, leave_type) — required for `ON CONFLICT DO NOTHING` seed inserts to work. Spec omitted this constraint.
- Session 13.6b: `leave_balances.balance` is a GENERATED ALWAYS AS column (`entitlement - used`) in PostgreSQL — SQLAlchemy model left as plain Column (no Computed) since no writes go through ORM for this column yet.
- Session 13.6b: EmployeeUpdate new salary fields use `Optional[Decimal] = None` (not `= 0.0`) to preserve blank=skip behaviour in partial updates.
- Session 13.7: `leave_balances.balance` is `GENERATED ALWAYS AS (entitlement - used)` — SQLAlchemy model now has `Computed("(entitlement - used)", persisted=True)`. Never include `balance` in LeaveBalance constructor.
- Session 13.7: Leave accrual idempotency key = `MONTHLY_ACCRUAL:YYYY-MM` stored in `leave_accrual_log.reason`. Calling run_monthly_accrual twice for the same month is safe.
- Session 13.7: CL/SL monthly pattern (2/1 alternating for months 1-9, 3/1 for months 10+) is driven by COUNT of prior CL `leave_accrual_log` entries, not calendar date. This means re-running accrual for past months in order will give correct cumulative pattern.
- Session 13.7: PL encashment deducts from `entitlement` (not `used`), so `balance = entitlement - used` naturally reflects the encashment. `encashed_ytd` is an additional audit trail.
- Current env: PostgreSQL runs NATIVELY (EDB PostgreSQL 18) not in Docker. `docker exec` won't work — use `/Library/PostgreSQL/18/bin/psql -U postgres -d udyogi_hrms -h localhost` with `PGPASSWORD=2399`. Docker daemon is running but has no containers.
- Block 8 / test data: UP000002–UP000006, US000001, US000002, UM000002 are test employees still in the DB. DELETE before deploying to VPS.
- Session 12: Payslip document card uses inline React styles (not Tailwind) intentionally — this preserves exact pixel-level parity with the WeasyPrint PDF template and ensures the Print media query renders identically.
- Session 12: Statutory calculator replicates `pt_resolver.py` client-side — effective-date filter + gender-priority sort. Uses `allRows` flattened from the live `/api/admin/statutory` response, so it always reflects DB values.
- Session 12: Payslip route is `/dashboard/payslips` (plural) — matches existing sidebar + dashboard quick-action links from Session 10.
- Session 10: Next.js 16 uses `src/proxy.ts` (not `middleware.ts`) with a named export `proxy` (not `middleware`). The `config.matcher` syntax is the same.
- Session 13.5: WeasyPrint must be pinned to >=69.0 in `requirements.txt` — 62.3 crashes on Python 3.14 (`AttributeError: 'super' object has no attribute 'transform'`). Production Docker image must also install pango (`apt-get install -y libpango-1.0-0 libpangocairo-1.0-0`).
- Session 13.5: `proxy.ts` matcher must exclude static file extensions (`.png .jpg .svg .woff` etc.) otherwise Next.js image optimizer and font requests are proxied through the auth guard and break.
- Session 13.5: `backdrop-filter: blur()` on any element creates a new CSS stacking context — child dropdowns/modals need `relative z-10` (or higher) on their own ancestor to escape it.
- Session 13.5: Logo: `udyogi-logo.png` placed in `frontend/public/` ✓ (confirmed Session 13 end)
- Session 13.5: super_admin active password: `Admin@2026` (not `Udyogi@2026`)
- Session 10: Tailwind v4 has NO `tailwind.config.ts`. Custom brand colors are registered in `src/app/globals.css` under `@theme { --color-udyogi-red: #E5202E; ... }` — these become Tailwind utility classes automatically (e.g. `bg-udyogi-red`).
- Session 10: Logo component (`src/components/Logo.tsx`) uses `<Image>` pointing to `/public/udyogi-logo.png`. That PNG does not exist yet — `LogoFallback` SVG is used everywhere as placeholder until the real logo is placed.
- Session 10: Auth flow is fully client-side. Tokens stored in `localStorage`. Proxy (server-side) reads `auth_session` non-httpOnly cookie (set on login via `document.cookie`). `useAuth()` hook does the real guard with `useEffect` + `router.replace`.
- Session 10: Entity context is a Zustand store (`src/store/entity.ts`). `super_admin` sees all 5 tabs (ALL + 4 entities); other roles see ALL + their own entity only.
- Session 11: `GET /api/admin/form-options` returns {entities, locations, departments, grades, shifts} — all with entity_id for client-side filtering. Used in Add + Edit forms.
- Session 11: Employee detail page uses `use(params)` (React 19 / Next.js 16 pattern) to unwrap async route params.
- Session 11: Bulk import wizard generates the download template client-side from a hardcoded TEMPLATE_HEADERS array (no backend endpoint needed). Validate returns {valid, errors, total_valid, total_error}.
- Session 11: Inline edit on detail page skips `entity_id` and `emp_code` fields (both immutable). Location/dept/grade/shift dropdowns are pre-filtered to employee's entity_id in edit mode.
- Session 9: Master data endpoint uses ORM Employee with joinedloads rather than a raw `v_employee_full` view query — same data, more resilient (no dependency on view existing in DB).
- Session 9: `GET /master-data` uses `alias="status"` on the query param to avoid shadowing Python's built-in `status` name imported from fastapi.
- Session 9: Column-update commit raises HTTP 404/403 mid-loop if any employee is invalid — this rolls back the entire transaction, enforcing "all succeed or all fail" per spec.
- Session 9: `reset-password` returns temp password in plain text in the response body only — it is never stored anywhere; `is_first_login=True` forces change on next login.
- Session 9: superadmin current password is `Admin@2026` (set during Session 4 testing). Run `scripts/create_superadmin.py` to reset to `Udyogi@2026`.
- Session 1: `verify_token` raises `HTTPException 401` instead of returning `None` — safer default, avoids silent auth bypass in callers.
- Session 1: CORS `allow_origins` is `["*"]` in development, empty list in production — controlled by `ENVIRONMENT` env var.
- Session 1: JWT `exp` uses `datetime.now(timezone.utc)` instead of deprecated `datetime.utcnow()`.
- Session 1: `psycopg2-binary` pinned at `2.9.12` instead of `2.9.9` — 2.9.9 has no prebuilt wheel for Python 3.14 (local dev machine). Docker build uses `python:3.12-slim` so both versions are compatible there.
- Session 4: superadmin test password is `Admin@2026` (changed from `Udyogi@2026` via create_superadmin.py + change-password during testing). Re-run `scripts/create_superadmin.py` to reset to `Udyogi@2026` if needed.
- Session 5: DB CHECK constraint on `employees.gender` requires lowercase (`male`, `female`, `other`). `commit_import` normalises via `.lower()`. Template CSV and upload files must use lowercase gender. Same applies to `status` (`active`, `inactive`, `exited`).
- Session 13.7: `EmployeeCreate` and `EmployeeUpdate` schemas now also normalise `gender`, `marital_status`, and `status` to lowercase via `field_validator(..., mode="before")`. Previously only bulk import did this — direct POST/PUT with uppercase gender would 500.
- Session 5: `generate_emp_code` called once per auto-coded row with `db.flush()` after each insert — ensures the new row is visible to the next call's SELECT FOR UPDATE within the same transaction.
- Session 6: ESIC uses `math.ceil()` not `round()` — fractional paise always round UP per statutory requirement (e.g. 70.12 → 71, not 70). This is intentional.
- Session 6: UM000001 seed salary corrected to `basic=5542, hra=2000, da=1000, spl=807, cca=0` (gross=9349) to match the CLAUDE.md validation test. Previous seed had gross=8500.
- Session 6: `generate_pdf()` in `pdf_generator.py` lazy-imports WeasyPrint to avoid startup crash on macOS dev machines missing Pango/Cairo. PDF endpoint works correctly inside Docker.
- Session 7: eSSL ADMS protocol sends each punch record as `ATTLOG PIN=x\tTime=...\tStatus=...\t...`. Parser strips the leading `ATTLOG ` prefix before parsing key=value pairs. A bare `ATTLOG` line (no fields) is skipped as a table-header.
- Session 7: `punch_type` column is `String(5)` — values used: `IN`, `OUT`, `OTIN`, `OTOUT`, `UNK` (Status 4=OT-In, 5=OT-Out).
- Session 7: `AttendanceRaw.created_at` and `BiometricMapping.created_at` set explicitly with `datetime.now(timezone.utc)` — no `server_default` on those columns (same pattern noted in Session 3 for `created_at` columns).
- Session 8: `payroll_months.total_deduction` is a PostgreSQL `GENERATED ALWAYS AS` column. SQLAlchemy model updated with `Computed("(((pf_emp + esic_emp) + pt) + loan_emi) + other_deduction", persisted=True)`. Do NOT pass `total_deduction` to the `PayrollMonth(...)` constructor — PostgreSQL owns it. The column can be read freely after INSERT/UPDATE. Same rule applies for any future `PayrollMonth` INSERTs.
- Session 8: `pay_days` in `payroll_months` is a plain column (not generated). Always set it explicitly from the attendance data or payroll engine.
- Session 8: Attendance CSV column mapping: `P`→`days_p`, `A`→`days_a`, `L`→`days_lwp`, `R`→`days_wo`, `C`→`days_cl`, `E`→`days_el`, `S`→`days_sl`, `H`→`days_h`, `OT Hours`→`ot_hours`.
- Logo: `udyogi-logo.png` placed in `frontend/public/` ✓ (confirmed Session 13 end)
- super_admin active password: `Admin@2026` (not `Udyogi@2026`)
- Session 13.11: `.animate-blob` etc. must be inside `@layer utilities {}` in globals.css — Tailwind v4 can silently override animate-* classes defined at top-level CSS scope.
- Session 13.13: `attendance_raw` check constraints: `punch_type` ∈ `{IN, OUT, UNKNOWN}`, `source` ∈ `{biometric, geo, manual}`. Web punch uses `source='manual'`. `attendance_daily.att_status` ∈ `{present, absent, halfday, late, lwp, cl, el, sl, holiday, wo}`.
- Session 13.13: `PunchCard` component is complete and tested but rendered from dashboard — re-enable by wrapping `<LeaveApplyCard>` back in a `grid grid-cols-1 lg:grid-cols-2` alongside `<PunchCard />`.
- Session 13.12: Punch-in/out (`POST /api/attendance/punch`) writes to both `attendance_raw` (source=`manual`, punch_type=`IN`/`OUT`) and `attendance_daily`. Auto-detects state: no first_in → punch-in; first_in exists → punch-out. Multiple punch-outs update `last_out` each time (last scan wins). OT threshold is hardcoded at 9h — may need to pull from Shift model in future. Biometric punches and web punches coexist in `attendance_raw`; `attendance_daily` is the authoritative daily summary for payroll.
- Session 13.12: Leave apply card is now visible to ALL roles on the dashboard (removed `!isAdmin` guard). Admins and HR can also apply for leave via the dashboard.
- Session 13.11: Always clear `.next` cache and restart dev server after multi-file sessions if UI changes are not reflecting. `rm -rf .next && npm run dev` is the fix.
- Session 13.11: `employees/page.tsx` catch block now logs errors + shows them in UI. If employees table shows a red error message, it will state the actual cause (e.g. "Invalid or expired token").
- Session 13.10: Login + change-password rebuilt as a faithful 1:1 port of the approved "liquid glass" Canva design. Shared scene extracted to `src/components/auth/LiquidGlassScene.tsx` (dark radial backdrop, 3 drifting oklch orbs, 2 chevrons, fractal-noise grain, vignette, 8 rising sparks, frosted card with entrance + sweep sheen + cursor specular). Both auth pages render their form as `children` inside it.
- Session 13.10: Fonts changed for the auth shell — **Saira** (headings/labels/buttons) + **IBM Plex Sans** (body/inputs), loaded via `next/font/google` in `layout.tsx` exposing `--font-saira` / `--font-plex`. Rest of app still uses Inter (`--font-inter`). `next build` confirmed fonts self-host fine.
- Session 13.10: **Logos** — one transparent logo, `/udyogi-logo.png`, used everywhere (login, change-password, dashboard `Logo` component). It's a clean cutout (~86% transparent, dark wordmark + red accents) and blends on light surfaces. The dark-theme white-flatten filter (`brightness(0) invert(1)`) was REMOVED from `Logo.tsx` (it killed the brand colors). On the dark sidebar the logo would be dark-on-dark, so the Sidebar header wraps it in a **light rounded "chip"** (`bg-white/95` pill) to keep its real colors legible.
- Session 13.10: **`Udyogi-logo-liq.png` is unusable as an overlay** and is NOT referenced anywhere — pixel decode showed ~43% of its pixels are semi-transparent gray (a frosted-glass panel baked into the PNG), which renders as a visible rectangular cutout/edge on any background. It's a standalone logo-on-a-card render; a true alpha-cut logo is needed if a "liquid" variant is ever wanted. Safe to delete the file.
- Session 13.10: The "alive" effects (pointer parallax, 3D card tilt, eased cursor-trailing glow, magnetic ripple SIGN IN button, Enter-to-submit) live in the `useEffect` inside `LiquidGlassScene` and target `[data-lg-*]` nodes. **Under `prefers-reduced-motion: reduce` the rAF glow loop, card tilt, layer parallax, and magnetic button are all skipped** (the static layout + entrance still render); globals.css also collapses scene animations + `.rise-in`/`.card-hover` for reduced-motion users.
- Session 13.10: App-wide motion layer added to `globals.css` — `--ease-expressive` / `--ease-smooth` tokens, `.rise-in` + `.delay-1..5` stagger, `.card-hover`, `.press`. Sidebar active item now shows a sliding `scaleX` red bar + staggered nav reveal; Header entity tabs/logout use `.press`; dashboard KPI + quick-action cards use `.card-hover` + staggered `.rise-in`.
- Session 13.10: `PasswordField` in change-password was hoisted to module scope (Next 16 `react-hooks/static-components` lint flags components defined during render — they remount and drop input focus each keystroke). `tsc --noEmit`, eslint, and `next build` all clean.
- **Session 13.5 — BUG FOUND + FIXED (auth status code):** an unauthenticated request to a protected endpoint (no `Authorization` header) returned **HTTP 403** instead of 401. Root cause: `HTTPBearer()` defaults to `auto_error=True`, which raises 403 when the header is missing. Fix: `dependencies.py` now uses `HTTPBearer(auto_error=False)` and `get_current_user` raises **401 "Not authenticated"** when `credentials is None`. 403 remains correct for authenticated-but-forbidden (`require_role`). Verified: no token → 401, bad token → 401, valid token → 200, wrong-role → 403.
- Session 13.5 — Prompt vs current-schema name drift (NOT bugs, just doc lag; tests were adapted): salary special-allowance column/field is **`spl`** not `special_al`; **`gross` is computed** (`basic+hra+da+spl+cca+other`), not a stored column — API exposes it as `monthly_gross`/`gross`. `employees.location_id` is a **varchar code** (`kol`,`jpr`,`dadra`…) not an int. `mobile` (10 digits) is **required** on create. `POST /api/payslip/process-month` is an **entity-wide batch** — body needs `entity_id` (not just `emp_code`); it returns `{processed, errors[]}` and isolates per-employee failures (a locked row lands in `errors[]`, whole batch still 200). Biometric mapping body is `{biometric_id, emp_code, device_sn, location_id}` (not `pin`). `next-code` returns key **`next_emp_code`**. Master-data export path is `/api/admin/master-data/export`. Public table count is now **23** (not 18) after later migrations.
- Session 13.5 — OBSERVATION (verify in Session 14, not fixed): a biometric punch sent as `Time=2026-04-01 09:00:00` was stored as `2026-04-01 14:30:00+05:30` — the naive device time is treated as **UTC** then shown in IST (+5:30). If eSSL devices push **local IST** time, punches shift +5:30 and can cross the date boundary near midnight, corrupting daily attendance. Confirm device time semantics before relying on `attendance_daily`. `attendance_daily` is also not auto-populated yet (no daily processor wired) — Block 5.4 returned 0 rows, expected.
- Session 13.5 — Block 6 (browser UI) NOT auto-verifiable in this environment. All page-backing APIs return 200 (`form-options`, `employees`, `attendance/monthly-summary`, `statutory`, payslip golden data) and `/login` serves with the new design; the visual/interaction items (entity-tab switching, add-employee form expand + live gross calc, PDF download button, clipboard copy, audit-log row expand, reduced-motion) still need a manual browser pass before deploy.

---

- Session 13.14: Pay period = 26th of previous month → 25th of current month. `get_period_dates(year, month)` in `services/period_calculator.py`. For month=1 → period_start is Dec 26 of prior year.
- Session 13.14: `PublicHoliday.location_id = NULL` means applies to ALL locations. `is_restricted=False` = mandatory (closed) holiday; `is_restricted=True` = optional/restricted holiday. Only mandatory holidays are subtracted from working_days.
- Session 13.14: Leave balance deduction on attendance import: attendance commit increments `leave_balances.used` and `taken_ytd` for CL/SL/EL. This is the authoritative deduction point — not on leave approval.
- Session 13.14: POST /api/leave/request requires `emp_code` in the body (employees pass their own; managers can pass others'). Employee role is blocked from submitting on behalf of others (403).
- Session 13.14j: System has EXACTLY 3 roles — `super_admin` (all entities), `entity_admin` (scoped to own entity_id), `employee` (own data only). `hr` and `manager` were removed everywhere (DB CHECK constraint, backend require_role, frontend UserRole type). The "admin tier" = super_admin + entity_admin (`isAdminRole`). Plain employees are restricted client-side via the centralized guard in `dashboard/layout.tsx` (allowed: `/dashboard`, `/dashboard/payslips`, `/dashboard/leave/apply`) and server-side via require_role + `_assert_access`.
- Session 13.14k: eslint reports 19 error-severity `react-hooks/set-state-in-effect` (+ a few unused-var warnings) — PRE-EXISTING across ~12 files, flagging the app-wide `useEffect`+`setState` auth/data-load pattern. `tsc` is clean. These would block a strict `next build` (Next runs eslint at build) — clean them up (or set the rule to warn) before the VPS deploy.
- Session 13.14i: Payslip endpoints recompute any UNLOCKED `payroll_months` row from current salary/attendance on every view/download — snapshots only freeze at `status=="locked"`. A salary edit therefore reflects immediately in the payslip/PDF without manual reprocessing. Locked rows are returned as-is (never passed to `process_payroll_month`, so no 400 risk).
- Session 13.14i / test data: current dummy employees in DB = UP000002, UP000003, US000001, UP000004, UM000002 (5 added this session). **DELETE these before VPS deploy.** Real fixtures to KEEP: UP000001 (super_admin, Priya Sharma) and UM000001 (golden test, Ramesh Kumar Singh — do not edit its salary). Earlier-session test rows (UP000002–UP000006 etc.) were already cleaned up; this is the current accurate list.
- Session 13.14h: To open the dev frontend on a phone over the LAN, BOTH must be set to the Mac's current LAN IP: `frontend/.env.local` → `NEXT_PUBLIC_API_URL` (so the browser calls the backend) AND `frontend/next.config.ts` → `allowedDevOrigins` (so Next serves `/_next/*` chunks to that origin — without it React silently fails to hydrate on mobile, all buttons dead). Also `next dev` runs with `--hostname 0.0.0.0` and uvicorn with `--host 0.0.0.0`. IP is DHCP — if it changes, update `.env.local` + `next.config.ts` and restart `npm run dev` (config changes need a full restart, not HMR).
- Session 13.14f: `auth_session` cookie must be cleared whenever tokens are cleared — if the cookie outlives the tokens, the proxy bounces the user to `/dashboard` on every hard reload, creating a redirect loop. `useAuth` and `logout()` both clear it now.
- Session 13.14e: `payroll_months.pay_days` has `NOT NULL` in DB but no `nullable=False` in the SQLAlchemy model — SQLAlchemy doesn't enforce it but PostgreSQL does. When no attendance rows exist, `pay_days` must default to `total_days` (never NULL). Any future attendance-optional reprocess path must follow the same rule.
- Session 13.14: `total_deduction` GENERATED ALWAYS column pattern also applies to `leave_balances.balance = entitlement - used` — never pass `balance` to LeaveBalance constructor.
- Session 13.14 (post-test bugfix): Old `/approve/{id}` endpoint was writing `lb.balance = ...` directly — crashes with `GeneratedAlways` PostgreSQL error because `balance` is `GENERATED ALWAYS AS (entitlement - used)`. Fixed by removing the `lb.balance` line; DB recomputes it automatically when `used` is updated. CORS error in browser was a secondary symptom (FastAPI 500 responses don't always include CORS headers, so browser reports it as a CORS block).
- Session 13.14: `attendance_daily.att_status` values in DB check constraint are lowercase (`present`, `absent`, `halfday`, `late`, `lwp`, `cl`, `el`, `sl`, `holiday`, `wo`). Payroll engine counts with `_count("P")` / `_count("WO")` etc. — **these must match whatever values are actually stored**. Template CSV uses P/A/C/E/S/H/R/L which the import service maps correctly; punch-in writes `'present'` lowercase.

- Session 13.15: `salary_structures` is the source of truth for pay components; the `employees` salary columns (basic/hra/da/spl/cca/leave_travel/other_allowance) are a synced CACHE of the currently-active structure (effective_to IS NULL). All existing read paths keep working unchanged because the cache mirrors the active row. `apply_increment` updates both.
- Session 13.15: increments ALWAYS take effect from the 26th (payroll-cycle start). `apply_increment` rejects any other day with 400 "Increment must be effective from the 26th (payroll cycle start)". One payslip = one structure; there is NO within-period proration of two rates.
- Session 13.15: `compute_payroll()` resolves the structure effective during the payroll period via `salary_resolver.get_structure_for_period` and uses it as the salary SOURCE; signature unchanged (resolution added inside it). Falls back to live employee columns when no structure exists (pre-history/safety). Processing a past month always uses that month's structure even after a later increment. Locked `payroll_months` snapshots are still frozen (rules 5 & 6) — doubly safe; unlocked past months now recompute correctly from history.
- Session 13.15: backfill used `doj` (the actual join-date column) — the spec's `date_of_joining` does not exist on this schema. Components were COALESCE'd to 0 because some employees had NULL spl/cca/etc and the new table columns are NOT NULL.
- Session 13.15: partial unique index `uq_salstruct_active ON salary_structures(emp_code) WHERE effective_to IS NULL` enforces one active structure per employee. It is non-deferrable, so `apply_increment` calls `db.flush()` after closing the old row (set effective_to) and BEFORE inserting the new active row, to avoid a transient two-active violation within one flush.
- Session 13.15: increment endpoints are `super_admin`+`entity_admin` only (entity-scoped). The spec mentioned `hr` but that role was removed in 13.14j. `GET /salary-history` additionally lets a plain `employee` view their OWN history.
- Session 13.15: ESIC rule is uniform for everyone — `esic_applicable = (statutory gross ≤ 21000)`. Already enforced everywhere in code (create/update/increment recompute the flag; `compute_payroll` computes ESIC directly from `gross ≤ 21000` and ignores the stored flag). One stale data row fixed this session: UP000001 had `esic_applicable=true` despite gross 45000 — corrected to false. All 7 employees now consistent (0 mismatches). Payslips were already correct (engine never trusted the flag); only the metadata field was stale.

- Session 13.17: payroll lock/unlock endpoints live in `app/api/payslip.py` on a SEPARATE `payroll_router` (the file's main `router` is mounted at `/api/payslip`; `payroll_router` is mounted at `/api/payroll` in main.py). Endpoints: `GET /api/payroll/months`, `POST /api/payroll/lock`, `POST /api/payroll/unlock`.
- Session 13.17: `payroll_months.status` ∈ draft/processed/locked. **Lock** sets `status='locked'`, `locked_at=now()` (idempotent re-lock is a no-op success). **Unlock** sets `status='processed'`, `locked_at=NULL`, and ONLY flips status — it does NOT recompute or wipe snapshots (rules 5/6). Reprocessing is a separate explicit `process-month` call. There is no `locked_by` column — actor + unlock reason live in `audit_log` (actions PAYROLL_LOCK / PAYROLL_UNLOCK) only.
- Session 13.17: Lock = super_admin + entity_admin (entity-scoped); **Unlock = super_admin ONLY + mandatory reason (≥5 chars, else 400)**. To extend unlock to entity_admin later, change the one `require_role("super_admin")` on `/api/payroll/unlock`.
- Session 13.17: aggregate month status rule in `GET /api/payroll/months` — all rows locked → 'locked'; all rows draft → 'draft'; otherwise 'processed'. `payroll_months` has no entity_id, so the query joins `employees` for entity scoping/grouping.
- Session 13.17: Apply-increment was MOVED off the employee detail page (now read-only there with a link to `/dashboard/payroll?emp={code}`) into the Payroll tab's Salary Structures sub-view. The increment modal + 26th-boundary validation behave identically to 13.15.
- Session 13.17: the new Payroll page uses `useSearchParams` (for `?emp=`) and is therefore wrapped in a `<Suspense>` boundary (Next 16 requirement). Plain employees can't reach it — sidebar item is `adminOnly` and the dashboard layout route guard only allows `/dashboard`, `/dashboard/payslips`, `/dashboard/leave/apply`.

## How to update this file
At the end of every Claude Code session, before /exit, paste:
"Update PROGRESS.md — mark Session N complete, log what was built and tested."
