# Copy to .env  (docker compose reads this for Postgres variable substitution).
# POSTGRES_PASSWORD must match the password inside backend/.env.production's DATABASE_URL.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=__STRONG_PASSWORD__
POSTGRES_DB=udyogi_hrms
